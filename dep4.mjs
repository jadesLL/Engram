import { saveChat } from './dist/lib/chat.js';
const content = `## 背景
角标长任务验证：一段多实体对话，用于触发较长 ingest 以观察任务队列角标 pop→clear 全过程。

## 对话
**用户**：介绍下「辰星计划」。

**Agent**：辰星计划是 2026 年 Q4 启动的数据中台项目，预算 600 万元，目标是统一数据出口。负责人是周婷，隶属于数据治理中心。预计 11 月完成一期上线。

**用户**：周婷是什么角色？

**Agent**：周婷是辰星计划的产品架构师，隶属于数据治理中心，同时负责「月影子项目」的接口设计部分。

**用户**：月影子项目是什么？

**Agent**：月影子项目是辰星计划的子项目，专注对外接口，预算 90 万，预计 12 月上线，承包方是「云栈科技」。

**用户**：决策与下一步？

**Agent**：决策：辰星计划的数据出口协议定为 GraphQL，由周婷主导。下一步：云栈科技需在 9 月 10 日前提交接口方案；月影子项目 10 月初启动实施。`;
const r = await saveChat({ content, identifier: '辰星计划讨论', project: '演示' });
console.log('DEPOSIT', JSON.stringify(r));
