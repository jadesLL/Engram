# 知识内容操作 AI 约定

本文件适用于应用内 Dream Cycle、入库管线，以及通过 MCP 读写 LLM Wiki 知识内容的外部 Agent。它不适用于修改本仓库源码的软件开发 Agent。

## 操作日志

- **位置**：`data/brain/Wiki/log.md`（frontmatter 标题「操作日志」）。
- **格式**：时间倒序，新条目插在 `# 操作日志` 标题正下方：
  ```
  - YYYY-MM-DD HH:MM:SS 动作：细节
  ```
- **原则**：条目保持原始一行式，不蒸馏、不汇总成状态看板。
- **资料入库**：`原始资料/` 下的对话、纪要和文件走正常入库管线，不创建绕过提炼的笔记类型。
- **来源版本**：同一路径以内容哈希形成不可变版本；新版本生效后，旧版本的 AI 管理贡献退出当前页面投影，但审计记录继续保留。
- **页面边界**：实体页固定为「当前理解 → 相关页面 → 时间线」。来源贡献只作为证据账本，不再按来源拼接到正文；页面级综合使用单一隐藏边界，保留边界外的用户自写内容。
- **整页综合**：实体页汇集全部有效来源事实后统一 Compose 与 Verify，按语义章节重写完整文档。检测到综合区人工修改时执行三方合并；无法确认人工内容已保留时进入待审，不覆盖当前页面。
- **来源查看**：段落与事实的证据映射保存在综合版本中，正文保持干净，通过页面来源侧栏查看资料路径、事实和原文引文。
- **追问闭环**：整理追问可回答、确认或忽略；回答会作为明确的用户补充证据重新进入 Normalize/Plan，而不是直接写入知识页。
- **跨来源门禁**：自动新建概念或实体页必须至少有两个不同 `原始资料/` 路径提供有效事实；同一路径的新版本仍只算一个来源。已有页面的增量更新不受该门禁限制。
- **待审操作**：批准或并入已有页面时，必须执行 `Retrieve → Recompose → Verify` 并展示差异预览；禁止直接写入旧草稿。
- **忽略语义**：忽略只关闭本次候选，不建立永久黑名单。资料更新或其他资料再次出现同名候选时，仍按完整入库路径重新判断。
- **LLM-first 边界**：总结、提炼、分类、消歧、重复/矛盾、关系、成熟度、时效和助手意图均由模型判断；代码只做解析、精确校验、schema、权限、状态、事务、队列、固定契约和明确业务门禁。详细阶段见 [`LLM-FIRST-ARCHITECTURE.md`](./LLM-FIRST-ARCHITECTURE.md)。

## 动手前 / 动手后

1. 对知识内容执行任何写操作前，先读取 `Wiki/log.md`，了解最近状态。
2. 操作完成后，将本次动作原样追加到 `Wiki/log.md`。
   - 应用内 AI 通过 `appendWikiLog(action, detail)` 自动写入。
   - MCP Agent 使用 `write_page`，把新条目插在标题正下方。

## 应用内自动写入点

| 触发 | 动作 |
|---|---|
| 新建页面 | `新建页面` |
| 归档 / 取消归档 | `归档` / `取消归档` |
| 删除页面或资料 | `删除` |
| 合并页面 | `合并`（原页已归档） |
| 回收站恢复 / 永久删除 / 清空 | `恢复` / `永久删除` / `清空回收站` |
| 死链批量建页 | `新建页面` |
| Dream Cycle 运行 | `Dream Cycle`（各分类计数完整保留） |
| 实体升级批次 | `实体升级`（所有实体完整列出） |
| 批量处理失败 | `批量处理失败`（所有错误完整保留） |
| 一键清除 | `清除`（重置操作日志） |

Dream Cycle 默认由 `server/src/dream/scheduler.ts` 定时触发，也可通过 `POST /api/dream/run` 手动运行。整理结果落入 `reports` 表，操作日志只记录运行摘要。

## 实际执行位置

- 操作日志写入：`server/src/pipeline/indexFile.ts`
- MCP 下发纪律：`server/src/mcp/server.ts`
- Dream Cycle 摘要：`server/src/dream/tasks.ts`
- 页面、文件、回收站和设置路由中的用户操作由相应服务端路由调用 `appendWikiLog`

`POST /api/settings/wipe-ai-logs` 会清理历史 `AIWorks/log/` 残留，重置 `Wiki/log.md`，并清空六词表关系边后重建空的 `Wiki/关系/relationships.md`。正常使用下不再创建独立 AI 日志文件。

`POST /api/settings/wipe` 除删除知识正文和索引外，还会清空 `reports`、任务历史、入库运行/事实/审计记录、Office 编辑会话与隐藏版本，并重置 `dream_last_run`；模型配置、密码、MCP Token、系统日志和回收站保留。
