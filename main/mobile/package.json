{
  "name": "@engram/mobile",
  "version": "0.0.0",
  "private": true,
  "description": "Engram 安卓本地优先客户端（内置 Vue UI、Ktor 本地服务和成员同步）",
  "scripts": {
    "prepare:web": "node scripts/prepare-mobile-web.cjs",
    "cap:sync": "cap sync android",
    "android:debug": "cd android && ./gradlew assembleDebug",
    "android:release": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.6.8",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.6.8",
    "@capacitor/preferences": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.8",
    "typescript": "^7.0.2"
  }
}
