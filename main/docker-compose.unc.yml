# 适用于 tsclient/UNC 远程目录环境的部署配置
# 说明：本环境下 Docker Desktop 无法绑定挂载 \\tsclient\D\...（"not a valid Windows path"），
# 因此改用命名卷 engram-data 持久化数据。该卷已含最新权威数据（wiki.db + brain）。
# 用法：docker compose -f docker-compose.unc.yml up -d --build
services:
  engram:
    image: engram:0.1.0
    build: .
    container_name: engram
    ports:
      - "18080:8080"
    volumes:
      - engram-data:/data
    env_file:
      - .env.onlyoffice
    environment:
      - TZ=Asia/Shanghai
      # 初次启动登录密码（仅首次生效；留空则首次登录时在页面设置）
      - DEFAULT_PASSWORD=${DEFAULT_PASSWORD:-}
      - OFFICE_EDITOR_ENABLED=true
      - OFFICE_INTERNAL_URL=http://onlyoffice
      - OFFICE_INTERNAL_APP_URL=http://engram:8080
      - OFFICE_PUBLIC_PATH=/onlyoffice/
      - OFFICE_INSTANCE_ID=main
    restart: unless-stopped
    sysctls:
      net.ipv4.tcp_mtu_probing: "1"
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:8080/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    depends_on:
      - onlyoffice

  onlyoffice:
    image: onlyoffice/documentserver:9.4.0
    container_name: engram-onlyoffice
    expose:
      - "80"
    env_file:
      - .env.onlyoffice
    environment:
      - JWT_ENABLED=true
      - JWT_HEADER=AuthorizationJwt
      - JWT_IN_BODY=true
      - ALLOW_PRIVATE_IP_ADDRESS=true
      - ALLOW_META_IP_ADDRESS=false
      - EXAMPLE_ENABLED=false
    volumes:
      - engram-onlyoffice-data:/var/www/onlyoffice/Data
      - engram-onlyoffice-lib:/var/lib/onlyoffice
      - engram-onlyoffice-logs:/var/log/onlyoffice
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost/healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 90s
    restart: unless-stopped

volumes:
  engram-data:
    external: true
  engram-onlyoffice-data:
  engram-onlyoffice-lib:
  engram-onlyoffice-logs:

networks:
  default:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: "${ENGRAM_NETWORK_MTU:-1400}"
