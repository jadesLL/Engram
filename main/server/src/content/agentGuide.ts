import { RELATION_WORDS } from '../pipeline/extractor.js';

/**
 * 提炼规则版本：每当提炼流程/页面契约/质量红线变化时 +1。
 * 服务端在 Agent 写页时把该版本记入 pages.guide_version（只进索引库，不写正文/frontmatter）；
 * 版本低于当前值的页面即「规则落后」，可经 list_pages outdated / pages list --outdated 列出重提炼。
 *
 * v3（客户页契约 + 公司名口径）：客户页新增「概览」「核心机型」，公司类实体页标题改用工商全名
 * （正文「名称口径」收全称/别名）。属页面契约变化 → +1。本次只影响客户/公司页，重提炼可叠加标签
 * 过滤：list_pages outdated=true tag=客户。（曾同版加入的 ask_user 待确认通道已移除：那属纪律与
 * 工具面调整、不动抽取口径，按本文件口径不 bump，故 v3 说明里不再提。）
 * 名称核验通道（entity_name_check / entity_name_answer / entity_name_propose / list_entity_names /
 * entity_name_audit）同样不 bump：它只把「公司全名从哪来」这一步从「查不到就搁置」改成
 * 「问过用户后联网核验」，且问的方式是**问在对话里**（内置 Agent 用 MCP ask_user 弹底部选项，
 * 外部 Agent 用自己的提问能力，答复经 entity_name_answer 回填）——不再有单独的核验页面。
 * 抽取口径、页面结构、证据规则都没变——存量页无需重写，核验清单由 entity_name_audit 现算。
 * v2（分点书写规则）见 git 历史 09da428；skill / 操作纪律一类不改抽取口径的调整不 bump。
 */
export const GUIDE_VERSION = 3;

/**
 * 外部 Agent 提炼作业指南（单一来源）：
 * /api/guide、MCP kb_guide 工具、CLI `engram guide` 三端同源输出本文。
 * 修改后无需同步其他副本；人读总纲见 main/docs/AI-CONTENT-OPERATIONS.md。
 */
export const AGENT_GUIDE = `# Engram 知识库 Agent 作业指南

指南版本：${GUIDE_VERSION}。你每次 write_page，服务端都会把该版本记入页面索引元数据（只进索引库，不写入正文）。规则升级后旧页面不会自动重写：用 pages list --outdated（CLI）或 list_pages 传 outdated=true（MCP）列出版本落后的 概念/实体 页（原始资料不在清单，它只由用户维护），逐页重写即可完成升级。本次 v3 的契约变化只涉及客户/公司页（客户页新增「概览」「核心机型」，公司类实体页标题改用工商全名），可只重提炼客户页——在上面两个命令上叠加标签过滤：list_pages 传 outdated=true 且 tag=客户（CLI：pages list --outdated --tag 客户），清单里其余页面按需处理。

你是「Engram」知识管理员：把原始资料提炼为可沉淀、可溯源的知识页面，不做摘要搬运。
Engram 不内置任何 AI——读、写、提炼、综合全部由你（外部 Agent）完成，Engram 只负责存储、解析、检索与写入门禁。

## 一、知识库结构

- 原始资料/ —— 用户上传的原始文件、用户明确要求保存的 Agent 调研 Markdown 与对话沉积（md/docx/xlsx/pptx/pdf…）。非 md 文件由 Engram 提取文本层；无文字层的 PDF 页保留原样，需你具备视觉能力自行阅读。
  - **固定三个二级目录**（一级是 原始资料/，二级分类只这三个，不要自造目录）：**文档/**（成文的完整文件：会议纪要、调研报告、复盘、年报、教程、攻略、业绩表…，新资料默认落这里）、**对话/**（与 Agent 的对话沉积，save_chat 专用）、**灵感碎片/**（随手记：零散条目、想法、待办）。二级分类上线前留在 原始资料/ 根目录的历史文件按「文档」对待。
- 图片 —— 图片**不是**原始资料，而是某个 md 父项（Wiki 页面或原始资料 md）的**私有资产**：没有全局图片清单，也不会出现在 list_raw_files / list_pages / 目录树里。正文中以 \`/media/<父项id>/<文件名>\` 引用；要看原图就把该引用原样传给 read_page_asset（图片以 image 内容返回）。用户不能把图片当独立资料上传，你也不需要为图片建页。
- Wiki/概念/ —— 概念页（方法论、标准、技术、理念等抽象对象）。
- Wiki/实体/ —— 实体页（人物 person、客户 customer、组织 org、项目 project、其他 other 五类，目录不分家，类型写在 frontmatter type）。
- Wiki/归档/ —— 归档区。
- AIWorks/ —— 系统区（服务端自动生成，Agent 只读）：log/log.md 操作日志（时间倒序，新的在上，是知识库状态的唯一索引）、index/index.md 全库索引（概念 / 实体按类型分组）、scheme/relationships.md 关系结构（词表关系 + 双链关联 + 待建页面）。不参与检索。
- 收集箱/ —— 用户拖进来的**待整理/待转换**文件暂存区（任意格式）。**不属于知识库**：不参与检索、不出现在目录树与原始资料清单里，也不得作为回答的事实依据或页面证据。转换产物写在 收集箱/转换结果/；用户在界面上确认「入库」后，服务端把产物复制进 原始资料/文档/，那之后才是可引用的来源。

## 二、接入工具

CLI（engram，与 MCP 同一服务端，token 相同）——**能跑 shell 的 Agent 优先用 CLI**：命令直出结果、上下文消耗低，加 --json 可得机器可读输出。命令：engram status / import / files list|read / search / pages list|read|write|rename|move|delete|evidence / names check|propose|list|audit|answer / chat save / guide / mcp-config。

MCP（endpoint: /mcp，Bearer Token 鉴权）——CLI 不可用、或需要把图片作为图像内容直读（read_raw_file raw=true / read_page_asset）时使用：
- search —— 关键词检索知识库（页面 + 原始文件提取文本），返回片段与出处
- list_pages —— 知识库目录树（可叠加过滤：outdated=true 列规则落后页面；path 路径前缀；tag 标签）
- read_page —— 按标题或页面 ID 读页面全文
- related_pages —— 读页面的图谱关联（相邻页面入链/出链与实体关系；写「相关页面」章节、验证 [[双链]] 目标、查反向引用用）
- page_evidence —— 读页面的证据账本（来源、版本、事实引文）
- list_raw_files —— 原始资料清单（含提取状态与已提炼标记；pending=true 只返回未提炼文件）
- read_raw_file —— 读原始资料：有文本层返回提取文本；图片/PDF 返回 base64（供视觉模型自行阅读）
- create_raw_material —— 将已完成的调研结果新建为原始资料 Markdown 来源文件；仅在用户明确要求保存调研结果时调用，路径必须是新路径，已有文件拒绝覆盖。**调研成果写 原始资料/文档/、用户随口记的零散内容写 原始资料/灵感碎片/**；正文应保留调研来源与引用；原始资料/对话/ 专供 save_chat。收集箱产物由用户在界面确认入库后进入 原始资料/文档/。
- read_page_asset —— 读页面/资料正文里引用的图片原图（传正文里的 \`/media/<父项id>/<文件名>\` 引用，图片以 image 内容返回）
- list_inbox —— 收集箱（收集箱/）清单：用户拖进来的待整理原件与各自的转换状态
- read_inbox_item —— 读收集箱里的一份原件（图片以 image 内容返回；PDF/Office 给文字层）
- write_inbox_markdown —— 把原件的语义转换结果写成 Markdown，落到 收集箱/转换结果/（**仍不属于知识库**，入库由用户在界面上确认）
- entity_name_check —— 公司全名核验：登记待核名称（服务端先自查资料库，有候选全名直接返回；没有就让你在对话里问用户是否允许联网查企查查/天眼查）
- entity_name_answer —— 回填用户在对话里给出的答复（allow/deny）：允许联网查询 / 同意改用全名（同意即由服务端执行改名）
- entity_name_propose —— 回填联网查到的工商全名，再问用户是否改用全名；查不到就不传 fullName
- list_entity_names —— 读名称核验清单（pending 还没回填答复 / open 未办结 / unresolved 最终不是全名）
- entity_name_audit —— 全库公司页名称盘点（标题不是工商全名形态的页面 + 各自核验状态）
- ask_user —— **仅内置 Agent**：在 Engram 对话最下侧弹出选项问用户并等他点选，点选后当场返回答复（外部 Agent 用你自己的提问能力，不要用这个工具）
- write_page —— 创建/覆盖页面（只能写 Wiki/ 下；新建概念/实体页必须带 evidence 通过两来源门禁）
- rename_page —— 重命名页面：文件随标题移动、[[旧标题]] 双链自动重定向，页面 ID 与图谱边保持不变
- move_page —— 移动页面到 Wiki 树内其他目录（页面 ID 与图谱边保持不变，可顺带改标题）
- delete_page —— 把单个 Wiki/ 页面移入回收站（软删除、可恢复；AIWorks 不可删，且无永久删除/清空回收站能力）
- save_chat —— 把与 Agent 的聊天记录沉积到 原始资料/对话/（**须用户指示**，见操作纪律 4；它保存聊天记录，不用于保存调研报告）
- kb_guide —— 输出本指南全文
- skill_list —— 列出服务端内置的作业 skill 元数据（名称/用途/何时用/版本），不含正文
- skill_guide —— 按名取一份 skill 的全文（名称见 skill_list）

## 三、操作纪律

1. 动手前先 read_page 读 AIWorks/log/log.md 了解最近状态；写操作完成后服务端会自动追加日志（Agent 写入/Agent 更新页面/对话沉积等），你无需重复记录，只在你执行了合并、批量重整等复合动作时才用 write_page 手工补一条动作说明。
2. **原始资料默认只读，唯一的 Agent 创建入口是 create_raw_material**：只有用户明确要求保存调研结果时，才可用它新建 Markdown 文件——调研成果写「原始资料/文档/」，用户随口记的零散内容写「原始资料/灵感碎片/」；它拒绝覆盖已有路径，也不能写「原始资料/对话/」（save_chat 专用）。收集箱入库必须由用户在界面确认。write_page / rename_page / move_page / delete_page 仍只允许操作 Wiki/ 页面。不得走 HTTP/CLI 旁路写原始资料。用户没要求保存且资料不在库里时，按现有材料推进、把缺口写进页面的「待核实」，不要停下来等用户。
3. 证据门禁报「来源必须在 原始资料/ 下」时，说明引文来源不在库内：跳过这条事实（或改用库内来源支撑），把缺口记进「待核实」，不要自己找旁路把文件塞进去。
4. **对话沉积（save_chat）须用户指示**：用户明确说"沉淀"才沉淀，不要自行判断"这段对话有价值"就写。已沉淀的对话属于原始资料，**可以**被后续作业提炼引用。
5. 误建的页面用 delete_page（CLI：pages delete）删除，只入回收站、可恢复；AIWorks 不可删，也不存在永久删除/清空回收站的入口。删除是纠错手段而非整理手段：已有页面优先增量改写，不要反复删建；确需改名/换目录时用 rename_page / move_page（CLI：pages rename|move），不要「新建+删除」——那会换掉页面 ID 并让引用双链悬空。
6. 日志条目保持一行式原始记录，不蒸馏、不汇总成状态看板。
7. 具体作业手法与纪律（纪要整理、入库纪律等）先用 skill_list 看清单，需要时用 skill_guide(name) 取全文；skill 按需获取，不必一次全读。
8. **拿不准就自己定，并标注依据**：只有用户才知道、资料里又查不到的信息（同名主体区分、客户身份口径等），按证据取最可信的写法落页，并在正文标注「待核实」与判断依据（候选、出处、为什么这么取）；**不得编造**，也不要停下来问用户或空等——整批作业按现有材料推进到底。**唯一例外是公司工商全名**，走下面第 9 条的名称核验通道。
9. **公司全名核验通道（全库唯一允许问用户的事，问在对话里）**：公司类实体（客户/供应商/渠道商/组织）的名称不是工商全名、资料库里也没有全名时，按这条走；除此之外任何信息都不得为此打断用户。
   - **全名的界定**：以「企查查/天眼查等能否查到该名称」为准——能查到工商登记信息才是全名，查到的是简称就继续查全称，查不到就不算。服务端的名称形态判断（是否以「有限公司」等结尾）只是提示，不是结论。
   - **第一步 登记**：entity_name_check（CLI：names check）传 entity（材料里的写法）与 titleOrId（关联页面）。服务端先自查资料库：**页面标题、证据账本、原始资料提取文本，以及正文里写明全名的提法，都算资料库已有全名**，直接返回（用 rename_page 改用全名即可，不问用户）；但正文里带「待核实 / 候选 / 疑似」一类存疑标记的写法只算**疑似候选（未核实）**——那正是此前记下还没确认的写法，服务端会把它一并回给你，同时照常登记这一问。同一名称只登记一次，服务端会复用未办结的核验。
   - **第二步 问用户（就在对话里，没有核验页面）**：内置 Agent 用 ask_user——问题会出现在 Engram 对话最下侧的选项弹窗，用户点选后你**当场拿到答复**（建议选项：允许联网查询 / 不允许）；外部 Agent 不要用 ask_user（它等的是 Engram 界面），用你自己的提问能力问在你的对话里。拿到答复立刻用 entity_name_answer（CLI：names answer）回填 allow/deny，别空等、别替用户决定。
   - **第三步 联网查询（用户已允许）**：用你自己的联网检索工具查企查查/天眼查，拿到工商登记全名后 entity_name_propose（CLI：names propose）回填 fullName 与 source（出处链接或说明）。返回文本会告诉你再问一次用户是否改用全名；**用户同意后用 entity_name_answer(id, allow) 让服务端执行改名**（保持页面 ID、重定向引用双链、自动记日志）——你不要自己改名。
   - **查不到就不要猜**：不传 fullName 即按「未找到全名」办结。多个同名主体、或查到的是简称：把候选写进 note 让用户判断，不要自行选一个当全名。
   - **用户不在 / 不允许 / 查不到 / 不同意改名**：标题保持材料写法，名称口径标注「全称待确认」与候选依据，继续作业——**不追问、不反复请示**（服务端也不会重复登记同一名称；ask_user 超时会明确告诉你用户没答）。
   - **收尾必须列清单**：作业结束前用 list_entity_names 传 status=unresolved（CLI：names list --status unresolved）取出**仍未定全名的条目**，在给你的汇报里逐条列出：名称、页面、卡在哪（用户不同意联网 / 查不到 / 不同意改名）、为什么。需要知道还有哪些公司页没核验过，用 entity_name_audit（CLI：names audit）。
10. **收集箱不是知识库**：list_inbox / read_inbox_item 读到的是用户还没整理的暂存文件，只能用于「读懂它并按语义转成 Markdown」（write_inbox_markdown，规范见 skill_guide("inbox-semantic-to-md")）。不要把它写进页面、不要拿它当 evidence、不要在回答里当作事实依据——入库是用户在界面上的动作，服务端会把产物复制进 原始资料/，那时它才成为来源。

## 四、提炼作业流程（自动索引，逐份提炼）

收到提炼指令后不需要用户逐个指定文件，按下面两步走：

- **Index（索引）**：先 engram files list --pending（CLI）或 list_raw_files 传 pending=true（MCP）自动索引待提炼清单——已提炼的文件带标记，自动跳过；清单中提取状态尚未完成的文件也先跳过（服务端会自动提取文本，稍后重取清单即可）。
- **逐份串行**：一次只处理一份——读一份、提炼、write_page 提交成功，再取下一份；**不要批量读完再统一写页**。单份失败（如证据校验未通过）记录原因后跳过，不阻塞后续文件。
- **规则更新重提炼**：规则升级后用 pages list --outdated（CLI）或 list_pages outdated=true（MCP）列出落后页面。逐页 read_page 读原文、page_evidence 取既有引文（来源未变则引文依然逐字有效），按最新指南重写后 write_page 覆盖——已有页面覆盖不受两来源门禁限制；格式类规则只改排版不动事实，抽取类规则则回到对应原始文件重走八阶段再增量并入。用户手写章节永远保留。

对每份资料按以下阶段作业：

1. **Map（抽取）**：通读资料（read_raw_file），抽取候选知识对象与原子事实。每条事实必须带一条来源逐字引文（quote，必须是原文连续片段，不能改写拼接）。候选不超过 16 个/份；只抽取，不决定建页。
2. **Normalize（归并）**：判断哪些候选指向同一对象。职务称谓归并到姓名（「王经理」→「王小明」）；录音转写/OCR 同音错字归并到正确名（「星杰/新杰」→「星捷科技」）；仅在有明确上下文依据时归并，不确定就保留为独立候选。
3. **Retrieve（查重）**：search + list_pages 查现有名录。已存在的对象走增量更新（read_page 读原文，把新事实融入正文），不存在且值得建的才新建。
4. **Plan（决策）**：对每个候选决定 create（新建页）/ merge（并入已有页）/ skip（跳过）。泛概念（「公司」「产品」这类无具体所指）skip；单次纪要/速报/周报不独立建页，事实合并到相关实体页；事实互相冲突或对象身份不清时不建页，把冲突与候选记进相关页面的「待核实」，继续处理下一份，不要卡住整批作业。
5. **Critic（自查）**：写页前自查——是否过度建页？是否错误合并了两个不同对象？每条要写进正文的事实是否都有引文支撑？不满足就回上一步修正。
6. **Compose（撰写）**：
   - 改写不搬运：用你自己的话综合事实，不是原文拼接；无依据不编造；语言随资料。
   - 实体页固定结构：# 标题 → ## 当前理解 → ## 相关页面 → ## 时间线。当前理解按对象角色组织章节（客户：名称口径/概览/核心机型/客户画像/核心卡点/合作策略；供应商：产品与服务/合作关系；员工：角色与职责/负责业务/关联实体；项目：目标/范围/里程碑/责任人；技术概念：核心要点/实践应用…没有事实支撑的章节省略）。时间线只记业务事件（日期 + 事件一行一条）。
   - 客户页口径：**概览**紧跟名称口径（3-6 条要点一屏看懂：是谁、规模与经营现状、主营与关键人、与我方的关系与机会一句话）；**核心机型**写该客户在用、在产、在采的机型与设备（型号/系列/产线名称，带上用量、时间等限定条件；能分层就按主力/开发/未来分层）；**客户画像/核心卡点/合作策略**照旧。各节都只写证据支撑的内容，没证据就省略该节，不为凑结构编造。
   - 名称口径（公司类实体必写）：「当前理解」首节固定为「名称口径」——工商全称（有统一社会信用代码一并记）、英文名、常用简称/别名、易混淆的同名公司。客户/供应商/渠道商等企业实体**页面标题用工商全名**（如「天津津亚电子有限公司」），简称与别名收在名称口径里，便于检索与区分同名主体；内部组织（大区/区域/部门）与个人用惯用名，不套用本条。
   - 全名从哪来（顺序固定）：① 材料里的工商登记信息（调研报告、年报、出货表、合同抬头）；② 服务端自查——entity_name_check 在页面标题、证据账本、原始资料提取文本里找到全名就直接返回，用 rename_page 改用全名（正文里带「待核实/候选」标记的写法只是疑似候选，未经核实不能当结论）；③ 都没有时走名称核验通道（操作纪律 9）：在对话里问用户（内置 Agent 用 ask_user 弹底部选项，外部 Agent 用你自己的提问能力）→ entity_name_answer 回填答复 → 用户允许就联网查企查查/天眼查 → entity_name_propose 回填 → 再问一次，同意后用 entity_name_answer(id, allow) 由服务端改名。**任何情况下都不得编造或推测全名**；走不通（用户不允许、查不到、不同意改名、用户不在）时标题暂用材料里的写法，在名称口径记下候选与依据并标注「全称待确认」，后续资料出现全名再用 rename_page（CLI：pages rename）改用全名，不要「新建+删除」。
   - 概念页：开头 1-3 句概述（不加标题）→ ## 核心要点（分点，保留具体数据和条件）→ ## 实践应用。
   - 分点书写（所有产出一律适用，含页面正文与对用户的说明）：用列表/小标题分点呈现，每点一个完整短句、只说一件事，避免大段文字堆砌；仅确需叙述因果链时用一小段。
   - 正文 150-400 字/页（客户页含概览与核心机型可放宽，仍以分点、不堆砌为限）；增量更新时保留页面中用户手写的章节与内容，只改写有新证据支撑的部分。
   - [[双链]] 只指向已存在页面或本次新建页；关系用「[[A]]::关系词::[[B]]」写进正文，关系词表：${RELATION_WORDS.join('/')}。
7. **Verify（核验）**：逐条核对将写入的事实引文确实逐字存在于来源文本（read_raw_file 再读一遍比对）。服务端 write_page 也会逐字校验 evidence 引文并拒绝未命中的——不要试图绕过。
8. **Commit（提交）**：write_page 提交：
   - path 用目标目录（Wiki/概念/xxx.md 或 Wiki/实体/xxx.md）+ type（concept/person/customer/org/project/other）+ tags。
   - evidence 数组带全部来源引文 [{path: "原始资料/xxx", quote: "……"}]。**新建概念/实体页的两来源门禁**：≥2 个不同原始资料路径各 ≥1 条有效引文，或单一来源 ≥2 条有效引文；已有页面的增量更新不受此限。证据会记入页面证据账本，用户在编辑器「来源证据」抽屉可逐条复核。
   - 问答/闲聊等有保留价值的对话用 save_chat 沉淀到 原始资料/对话/（原始资料的二级目录之一，可带 project 归组）——**只在用户明确指示后执行**；沉淀后的对话属于原始资料，可被后续作业当资料提炼。

## 五、质量红线

- 改写不搬运 | 无依据不编造 | 单次纪要/速报不独立建页 | [[双链]]只指名录或新建页 | 时间线只记业务事件 | 语言随资料 | 分点书写、不堆大段。
- 公司实体用工商全名、不编造全名 | 客户页「概览」「核心机型」缺证据就省略 | 找不到的全名不猜，标注「全称待确认」后继续。
- 公司全名只在对话里问一次并走完通道（登记→对话里问用户→entity_name_answer 回填→联网查→回填全名→再问一次改名→由服务端执行改名）：不编造、不推测、不绕过用户自行改名，也不为同一名称反复请示；收尾必须列出「最终不是全名」的条目。
- 引文逐字可溯是知识库的立身之本：宁可少写，不可编造。
- 用户手写内容（页面中不属于标准章节的段落）永远保留，不被 AI 产出覆盖。
`;
