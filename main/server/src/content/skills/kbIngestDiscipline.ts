import type { SkillDoc } from './types.js';

export const SKILL_VERSION = 2;

/**
 * 知识库入库纪律。
 * 与《Agent 作业指南》同源口径：原始资料对 Agent 是「无写权限」，不是「不存在写入口」；
 * v2：去掉「先问用户 / 登记待确认问题」的全部路径——拿不准就自己定并标注「待核实」。
 */
export const SKILL: SkillDoc = {
  name: 'kb-ingest-discipline',
  title: '知识库入库纪律',
  description: '沉淀对话、建页删页前的顺序纪律，以及证据门禁被拒、拿不准时的自主处理口径。',
  whenToUse: '准备向知识库写入任何内容（save_chat、write_page、删除页面）或遇到门禁/资料缺口之前。',
  version: SKILL_VERSION,
  body: `# 知识库入库纪律

## 一、原始资料：Agent 没有写权限
- \`原始资料/\` 与 \`AIWorks/\` 是只读区：Agent 的 \`write_page\` / \`rename_page\` / \`move_page\` / \`delete_page\` 只允许 \`Wiki/\` 下的页面。
- 但要注意**软件本身**具备上传、新建、删除原始资料的能力（Web 界面与 REST API）。
  「只读」约束的是 **Agent 的权限**，不是文件不可改——**不得因此走 HTTP 旁路**。
- 作业时需要的资料不在库里：按现有材料推进，把缺口写进页面的「待核实」，**不要停下来等用户**，更不得自行写入资料区。
- 权限 ≠ 授权：即使运行环境给了完整文件权限，也不代表可以自行写入用户的资料区。

## 二、证据门禁被拒怎么办
- 门禁报「证据来源必须在 原始资料/ 下」时，说明引文来源不在库内。
- 正确动作：跳过这条事实（或改用库内来源支撑），把缺口记进页面的「待核实」，继续处理下一份。
- 错误动作：自己找 HTTP/CLI 旁路把文件塞进去。这属于越权，即使技术可行。
- 门禁设计意图就是「宁可拒绝，也不让假证据进账本」，绕过它等于绕过质量底线。

## 三、对话沉积（save_chat）：须用户指示
- **触发权在用户**：用户明确说"沉淀"才沉淀。
- **禁止主动沉淀**：不要自行判断"这段对话有价值"就往 \`原始资料/对话/\` 写。
- 沉淀之后**可以被提炼**：对话一旦沉淀即属原始资料，与普通资料同等对待，可被后续提炼作业引用。
- 一句话：卡的是「谁决定沉淀」，不是「沉淀后能不能用」。

## 四、建页与删页
- 单次纪要/速报/周报**不独立建页**，事实并入相关实体页。
- 已有页面优先**增量改写**，不要反复删建——删建会换掉页面 ID 并让引用双链悬空。
- 改名/换目录用 \`rename_page\` / \`move_page\`，不要「新建 + 删除」。
- 误建页才用 \`delete_page\`（只入回收站，可恢复）；删除是纠错手段，不是整理手段。

## 五、证据链换源顺序
需要把页面证据从旧来源改到新来源（例如原件被替换）时，顺序固定：
1. 先确认新来源已入库。
2. 逐页 \`write_page\` 重写，evidence 指向新来源，引文按新来源**逐字**。
3. 校验通过后，再删除旧来源。
- 反过来做会让页面证据全部悬空。

## 六、不确定就标注，不要抛问题
- 无依据的事实不写；无法判定的写「原文无据」，不要编造。
- 能自查的先自查（检索、读原文比对）；仍无定论就按证据取最可信的写法落页，并在正文标注「待核实」与依据（候选、出处、为什么这么取）。
- 不要停下来问用户、不要空等：整批作业按现有材料推进到底，拿不准的留给「待核实」后续用新资料收敛。

## 七、公司工商全名与主体身份：自己定，不猜也不问
- 材料里没有全名时先 \`search\` 全库，看其他资料（调研报告、年报、出货表、合同抬头）是否出现过。
- 仍找不到：**不编造、不推测全名**；标题暂用材料里的写法，在「名称口径」记下候选与依据并标注「全称待确认」，继续作业。
- 后续资料出现全名时再用 \`rename_page\` 改用全名，不要「新建 + 删除」。
- 同名主体区分、客户身份口径同理：按证据取最可信口径落页并标注依据，不阻塞。

## 八、每次写入前后
- 写操作前：读 \`AIWorks/log/log.md\` 了解最近状态。
- 写操作后：服务端自动记日志，无需手工重复；只有合并/批量重整才补一条动作说明。
`,
};
