import type { SkillDoc } from './types.js';

export const SKILL_VERSION = 4;

/**
 * 知识库入库纪律。
 * 与《Agent 作业指南》同源口径：原始资料对 Agent 是「无写权限」，不是「不存在写入口」；
 * v2：去掉「先问用户 / 登记待确认问题」的全部路径——拿不准就自己定并标注「待核实」。
 * v3：唯一的例外回来了，但只此一条——公司工商全名：entity_name_check 登记 → 在对话里问用户
 * （内置 Agent 用 ask_user 弹底部选项，外部 Agent 用自己的提问能力）→ entity_name_answer 回填答复
 * → 用户允许后联网查企查查/天眼查 → entity_name_propose 回填 → 再问一次，同意后服务端改名；
 * 其余信息一律不打断用户。
 * v4：新增 create_raw_material，仅在用户明确要求保存调研结果时创建全新 Markdown 原始资料；不覆盖已有文件，聊天记录仍走 save_chat。
 */
export const SKILL: SkillDoc = {
  name: 'kb-ingest-discipline',
  title: '知识库入库纪律',
  description: '保存对话或调研资料、建页删页前的顺序纪律，以及证据门禁被拒、拿不准时的自主处理口径。',
  whenToUse: '准备使用 create_raw_material、save_chat、write_page 或删除页面，或遇到门禁/资料缺口之前。',
  version: SKILL_VERSION,
  body: `# 知识库入库纪律

## 一、原始资料：默认只读，调研写入受限
- \`AIWorks/\` 对 Agent 只读；\`write_page\` / \`rename_page\` / \`move_page\` / \`delete_page\` 只允许操作 \`Wiki/\`。
- Agent 默认对原始资料没有写权限；只有用户明确要求保存调研结果时，才可调用 \`create_raw_material\` 在 \`原始资料/\` 下新建 Markdown。已有路径一律拒绝覆盖，也不能写 \`原始资料/对话/\`。收集箱产物仅在用户确认入库后进入原始资料根目录。调研正文应包含来源和引用，方便之后核验。
- \`save_chat\` 专门保存与 Agent 的聊天记录到 \`原始资料/对话/\`；不要用它保存调研成果，也不要用 \`create_raw_material\` 保存聊天记录。
- 不得走 HTTP/CLI 旁路写资料区。用户未要求保存、而作业需要的资料不在库里时，按现有材料推进，把缺口写进页面的「待核实」，**不要停下来等用户**。
- 权限 ≠ 授权：即使运行环境给了完整文件权限，也不代表可以自行写入用户的资料区。

## 二、证据门禁被拒怎么办
- 门禁报「证据来源必须在 原始资料/ 下」时，说明引文来源不在库内。
- 正确动作：跳过这条事实（或改用库内来源支撑），把缺口记进页面的「待核实」，继续处理下一份。
- 错误动作：自己找 HTTP/CLI 旁路把文件塞进去。这属于越权，即使技术可行。
- 门禁设计意图就是「宁可拒绝，也不让假证据进账本」，绕过它等于绕过质量底线。

## 三、对话沉积（save_chat）：须用户指示
- **触发权在用户**：用户明确说"沉淀"才沉淀。
- **禁止主动沉淀**：不要自行判断"这段对话有价值"就往 \`原始资料/对话/\` 写。
- 沉淀之后**可以被提炼**：对话一旦沉淀即属原始资料，与普通资料同等对待，可被后续提炼作业引用。
- 一句话：卡的是「谁决定沉淀」，不是「沉淀后能不能用」。

## 四、建页与删页
- 单次纪要/速报/周报**不独立建页**，事实并入相关实体页。
- 已有页面优先**增量改写**，不要反复删建——删建会换掉页面 ID 并让引用双链悬空。
- 改名/换目录用 \`rename_page\` / \`move_page\`，不要「新建 + 删除」。
- 误建页才用 \`delete_page\`（只入回收站，可恢复）；删除是纠错手段，不是整理手段。

## 五、证据链换源顺序
需要把页面证据从旧来源改到新来源（例如原件被替换）时，顺序固定：
1. 先确认新来源已入库。
2. 逐页 \`write_page\` 重写，evidence 指向新来源，引文按新来源**逐字**。
3. 校验通过后，再删除旧来源。
- 反过来做会让页面证据全部悬空。

## 六、不确定就标注，不要抛问题
- 无依据的事实不写；无法判定的写「原文无据」，不要编造。
- 能自查的先自查（检索、读原文比对）；仍无定论就按证据取最可信的写法落页，并在正文标注「待核实」与依据（候选、出处、为什么这么取）。
- 不要停下来问用户、不要空等：整批作业按现有材料推进到底，拿不准的留给「待核实」后续用新资料收敛。
- 唯一例外是名称核验（公司工商全名，见第七节）：材料与资料库都没有全名时才在对话里问用户，其余任何信息都不打断用户。

## 七、名称核验：公司工商全名是唯一的例外，问在对话里
- 全名的界定：以「企查查/天眼查等能否查到该名称」为准——能查到工商登记信息才是全名，查到的是简称就继续查全称，查不到就不算。服务端的名称形态判断只是提示。
- 顺序固定：① 材料里的工商登记信息；② \`entity_name_check\` 让服务端自查资料库（有候选全名直接返回，用 \`rename_page\` 改用全名）；③ 都没有才在对话里问用户。
- 问法（没有核验页面）：内置 Agent 用 \`ask_user\`——问题弹在 Engram 对话最下侧的选项框，用户点选后**当场拿到答复**；外部 Agent 用你自己的提问能力问在自己的对话里（不要用 \`ask_user\`，它等的是 Engram 界面）。拿到答复立刻用 \`entity_name_answer\`（CLI \`names answer\`）回填，别空等、别替用户决定。
- 用户允许联网后：用你自己的联网检索查企查查/天眼查 → \`entity_name_propose\`（CLI \`names propose\`）回填全名与出处 → 再问一次是否改用全名 → 同意后用 \`entity_name_answer(id, allow)\` 让**服务端改名**（保持页面 ID、双链重定向、自动记日志），不要自己改名。
- 同一名称只登记一次；对方超时未答（\`ask_user\` 会明确告知）就按现有材料推进，不要反复追问。
- 用户不允许、查不到、不同意改名、用户不在：标题保持材料写法，名称口径标注「全称待确认」与候选依据，继续作业——**不追问、不反复请示**。
- **不得编造或推测全名**，也不得绕过用户自行改名；收尾用 \`list_entity_names --status unresolved\` 把仍未定全名的条目列给用户。
- 除公司全名外的任何信息（同名主体区分、客户身份口径、资料缺口）一律不问用户：按证据取最可信口径落页并标注依据，不阻塞。

## 八、每次写入前后
- 写操作前：读 \`AIWorks/log/log.md\` 了解最近状态。
- 写操作后：服务端自动记日志，无需手工重复；只有合并/批量重整才补一条动作说明。
`,
};
