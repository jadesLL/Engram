import type { SkillDoc } from './types.js';
import { fixKindTableMarkdown } from '../fixRules.js';

export const SKILL_VERSION = 1;

/**
 * Word / 录音纪要 → Markdown 作业法。
 * 正文只写「怎么做」，不写具体某次任务的内容；实例均来自真实作业中的判据。
 */
export const SKILL: SkillDoc = {
  name: 'docx-meeting-to-md',
  title: 'Word 纪要转 Markdown',
  description: '把 Word/录音转写纪要整理成 Markdown，并逐条列出错别字订正与待确认项。',
  whenToUse: '用户给一份 .docx/.pdf 纪要要求「整理成 Markdown」「查错别字」「改标题」时。',
  version: SKILL_VERSION,
  body: `# Word 纪要转 Markdown

## 适用
- 输入：会议/通话纪要（Word、录音转写、AI 摘要导出件）。
- 输出：一份 Markdown 文件，正文按原文结构重组，末尾附「订正记录」。
- 与入库（\`kb-ingest-discipline\`）是两件事：本 skill 只做文档整理，不动知识库。

## 一、结构转换
- 原文标题层级映射为 Markdown 标题（\`#\` / \`##\` / \`###\`），表格还原为 Markdown 表格。
- 原文的空行、缩进层级保留；列表用 \`-\`，子列表缩进两空格。
- 数字与时间统一半角（\`9:00\`、\`13:30\`、\`100 到 300 台\`），中英文之间按需补空格，但**不改动语义**。
- 表格里的 \`-\`、\`—\` 保留原样，不擅自改成"无"。

## 二、标题改写
- 先看原标题的问题：人名重复、带导出后缀（\`_AI 摘要\`）、含义残缺。
- 改成能自解释的纪要标题，例如「刘盈 与刘盈沟通客户会见行程安排_AI 摘要」→「与刘盈沟通北自所客户会见行程安排纪要」。
- 在报告的订正记录里写明**为什么改**，让用户能复核。

## 三、错别字四类判据
逐条列出「位置 / 原文 / 订正后 / 依据」，依据必须落在下面四类之一（判据与服务端记录灵感的自动勘误同源，见 content/fixRules.ts）：

${fixKindTableMarkdown()}

- 四类判据之外**不要改**：拿不准的写进「保留原文表述」并注明原因。
- 订正后要全文复查一遍，确认没有残留（用 grep 数一遍原文写法出现次数）。

## 四、人名与称呼归并
- 职务称谓归并到姓名（「王经理」→「王小明」）。
- 同音/异写归并（「侯程程」「候成程」→ 知识库既有写法「侯成程」）。
- **归并必须以用户确认为准**，或知识库已有明确口径；不能只凭推断。
- 归并结果写进订正记录，标明依据是「用户确认」还是「知识库既有名录」。

## 五、代词不是人名
- 「我方」「对方」「双方」是**代词**，不是人名。
- 名单里出现代词时按原样保留，**不要**列为待确认人名去问用户。
- 只有当同一位置明显应是具体姓名时才提请确认。

## 六、存疑项
- 先自查：原文上下文、知识库检索、外部资料核对。
- 查不到且属用户内部信息才问；问的时候给出你手上的全部上下文。
- 仍无据则写进「保留原文表述（未作改动）」，注明「原文无依据，未擅自改动」。
- 不要把自己的推断当问题抛给用户。

## 七、交付格式
1. Markdown 正文（按上述结构）。
2. 末尾「附录：订正记录」：
   - 表格：位置 / 原文 / 订正后 / 依据。
   - 「保留原文表述（未作改动）」小节：逐条说明为什么不动。
3. 汇报时说明：改了哪几类、哪些待确认、原文件是否动过。

## 八、红线
- 原文没写的不补（不猜漏字、不补人名）。
- 不确定不合并、不删改。
- 原文件保持原样；产出一律另存新文件。
`,
};
