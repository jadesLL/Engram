import type { SkillDoc } from './types.js';

export const SKILL_VERSION = 1;

/**
 * 收集箱 → Markdown 的语义转换作业法。
 *
 * 这份正文是**服务端一键转换与 Agent 转换共用的同一份规范**：
 * 服务端 job 把它当 system prompt 内联，Agent 经 MCP `skill_guide('inbox-semantic-to-md')` 取。
 * 两条通道的产物必须落在同一个位置、用同一套结构，用户不该看出是哪条通道转的。
 */
export const SKILL: SkillDoc = {
  name: 'inbox-semantic-to-md',
  title: '收集箱语义转换 Markdown',
  description: '把收集箱里的原件按内容语义重写成人类可读的 Markdown 文档，并落到 收集箱/转换结果/。',
  whenToUse:
    '用户说「转换收集箱里的内容 / 把收集箱那个文件转成 Markdown」，或需要读取收集箱原件内容时。',
  version: SKILL_VERSION,
  body: `# 收集箱语义转换 Markdown

## 适用与边界
- 输入：收集箱（\`收集箱/\`）里的一份原件，内容已经以文字形式拿到手里。
- 输出：一份 Markdown 文档，写到 \`收集箱/转换结果/<原名去扩展名>.md\`（MCP 用 \`write_inbox_markdown\`）。
- **收集箱不是知识库**：转换产物在用户点「入库」之前不参与检索、不被引用、不得作为事实来源。
- 本 skill 只管「把这份文件变成人能读的 Markdown」，**不**提炼概念、不写 Wiki 页面、不动证据账本。

## 一、这是语义转换，不是格式搬运
把原件的**信息**重写成 Markdown，而不是把字符原样倒出来：
- 表格要重建为 Markdown 表格，并把表头含义写清（\`Q3\` → \`2026 年第三季度\`）。
- 记录体裁（访谈、会议、销售报表、合同）要按该体裁的信息结构重组，
  例如访谈按「受访对象 → 现状 → 痛点 → 关注点 → 原话摘录」。
- 散乱的行、页眉页脚、导出噪声、重复段落要合并或去掉，**但不丢信息**。
- 扫描件里识别出来的文字可以补标点与断句，**不得补内容**。

## 二、必须保留的
- 数字、金额、日期、编号、人名、公司名：逐字保留，不做「合理推断」。
- 明确带引号的表述进「原话摘录」，一字不改。
- 原件里说不清、看不懂、疑似错字的地方：原样保留并在文末「待确认」里列出来，
  写清位置与存疑理由，**不要替用户猜**。

## 三、结构要求
1. 一级标题：能自解释的文档名（不要用 \`Document1\`、\`导出文件\` 这类）。
2. 开头 3–5 行「本文概要」：这份文件是什么、涉及谁、时间范围。
3. 正文按原件的信息层级分节；节标题用名词短语，不要用「第一部分」。
4. 表格、清单、时间线该用就用；长段落拆成要点。
5. 文末固定两节：
   - \`## 待确认\`：存疑项（没有就写「无」）。
   - \`## 原件信息\`：原文件名、格式、大小、页数/表数量等你能确定的元信息。

## 四、交付纪律
- 产物只写一份 Markdown，路径由调用方给定，不要另外建文件。
- 不要修改原件，也不要移动它。
- 不要在产物里写「由 AI 生成」之类的自我说明；元信息写在 frontmatter（服务端会补）。
- 内容太长被分成多段转换时，各段之间保持标题层级连续，不要重复概要。`,
};
