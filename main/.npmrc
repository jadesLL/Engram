# pnpm 装依赖时会跑 electron / better-sqlite3 的 postinstall，二者默认都去 GitHub 拉二进制；
# 国内直连不稳定，客户端全新安装会卡在 `electron postinstall: RequestError: read ECONNRESET`
# 以退出码 1 失败（2026-09-17 客户机实测）。这里的键会被 pnpm 以 npm_config_<键名> 的形式
# 传给生命周期脚本，正是两个下载器读取的键名：
#   @electron/get    → npm_config_electron_mirror
#   prebuild-install → npm_config_better_sqlite3_binary_host_mirror
# 值与本仓库既有代码（sync-deps.js 的 ensureNativeBinding / ensureElectronRuntime）保持一致，
# 改了这里无需改代码即可整体换源。
electron_mirror=https://npmmirror.com/mirrors/electron/
better_sqlite3_binary_host_mirror=https://registry.npmmirror.com/-/binary/better-sqlite3
