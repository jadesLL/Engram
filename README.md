<p align="center">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="main/docs/brand/engram-lockup-dark.svg" />
    <img src="main/docs/brand/engram-lockup-light.svg" alt="Engram" width="440" />
  </picture>
</p>

# Engram — 个人知识大脑（确定性内核 + Agent 驱动）

一个**内核不内置任何 AI** 的自托管知识库：Markdown 文件是权威数据源，Engram 负责存储、文档解析、关键词检索与**确定性写入门禁**；读、写、提炼、综合、问答由 Agent 通过 **MCP** 或 **CLI** 完成——既可以用你已有的外部 Agent（ZCode / Codex / Claude Code / Kimi / Cursor 等），也可以用 Engram **随包内置的 Agent**（内嵌 DeepSeek Harness 的聊天抽屉，配一把你自己的模型 Key 即可开口）。两条路都走同一套 MCP 工具与证据门禁；不配 Key 时内置 Agent 不出网，知识库依旧零 API Key。

## 工作流

```
你导入资料 ──► Engram 存储并提取文本层（PDF 文字层 / Office / md）
                    │
外部 Agent ◄───────┤  MCP 26 工具 或 engram CLI（同一 Bearer Token）
（ZCode/Codex/…）   ▼
              按指南作业：Map→Normalize→Retrieve→Plan→Critic→Compose→Verify→Commit
                    │
              write_page 带证据引文 ──► 服务端逐字校验 + 两来源门禁 + 记入证据账本
```

- **《Agent 作业指南》三端同源**：MCP `kb_guide` 工具、`GET /api/guide`、`engram guide` 输出同一份方法论（页面契约、八阶段作业流程、证据规则），Agent 接上就能按既有逻辑干活。
- **内置 skill 按需下发**：服务端内置作业手法（`skill_list` 列清单、`skill_guide` 取全文），如纪要转 Markdown、入库纪律；skill 版本独立于指南版本，改 skill 不把已有页面标为规则落后。
- **CLI 优先、逐份串行**：能跑 shell 的 Agent 优先用 CLI（MCP 兜底用于图像直读等场景）；收到提炼指令先用 `files list --pending` 自动索引待提炼清单，逐份提炼、写完一份再下一份。
- **规则版本化，旧库可升级**：提炼规则带版本号（`GUIDE_VERSION`），Agent 每次写页服务端把版本记入页面索引元数据（只进索引库，不写正文）；规则升级后用 `pages list --outdated`（CLI）或 `list_pages` 传 `outdated=true`（MCP）列出落后的概念/实体页（原始资料只读不改），按最新指南逐页重写覆盖即完成旧库升级。
- **质量由确定性门禁兜底**：引文逐字校验（编造即拒绝）、新建概念/实体页两来源门禁（≥2 个不同原始资料路径各 1 条引文，或单路径 ≥2 条）、每次写入自动记入 `Wiki/log.md` 操作日志与证据账本（编辑器「来源证据」抽屉可逐条复核）。
- **全自动、不打断**：作业全流程不问用户——资料里查不到的先自查全库，仍无定论就按证据取最可信写法落页，并在正文标注「待核实」与依据（不编造、不空等），后续资料补齐再收敛；客户/公司页按 v3 契约写「概览」「核心机型」与「名称口径」，标题用工商全名、别名收在名称口径里。
- **公司全名核验（唯一允许问用户的事，问在对话里）**：公司类实体的名称不是工商全名、资料库里也查不到时，Agent 经 `entity_name_check` 登记，然后**直接在对话里问你**——内置 Agent 的提问会弹在对话最下侧的选项框（点选即答，Agent 同一轮接着往下做）：① 先问你是否允许它**联网用企查查 / 天眼查**查全名；② 同意后它查回全名与出处，再问你是否**把页面标题改成该全名**，点「改用全名」即由服务端改名（保持页面 ID、引用双链自动重定向、自动记操作日志）。全名的界定就是「企查查等能否查到该名称」：不允许、查不到、不同意改名、超时未答都照旧保持材料写法并标注「全称待确认」，Agent 收尾会把**仍未定全名的条目**逐条列给你。外部 Agent（Claude Code / Codex / ZCode 等）用自带的提问能力问在自己的对话里，答复经 `entity_name_answer` 回填。除公司全名外，任何信息都不会打断你。

## 功能总览

### 📁 原始资料（一级目录 + 三个二级分类）

侧栏「原始资料」下固定三个二级分组：**文档**（成文的完整文件：会议纪要、调研报告、复盘、年报、教程、攻略、业绩表…）、**对话**（与 Agent 的对话沉积，`save_chat` 写入，可按项目再分子目录）、**灵感碎片**（随手记：零散条目、想法、待办）。三个分组各有折叠开关与计数，排序沿用分区级「原始资料排序」。

- **落点固定**：上传、拖入、新建 Markdown、收集箱「入库」都落「文档」；Agent 的 `create_raw_material` 保存调研成果落「文档」、用户随口记的零散内容落「灵感碎片」；「对话」只由 `save_chat` 写入（新建文件接口对它返回 403）。
- **编辑页「分类」下拉**：打开一份原始资料，页头的下拉显示的是**分类**（文档 / 对话 / 灵感碎片）而不是页面类型——换一个即把文件移到对应二级目录，页面 ID、图谱边与证据账本跟着走（按路径存的 `source_versions` / `ingest_runs` / `page_revisions` 一并改挂新路径，换分类后仍算「已提炼」，不会重提炼、来源不会变死链）。Wiki 页面照旧显示类型。
- **左下角「+」＝记一条灵感**：图标栏底部的「+」（与 `Ctrl+N`）弹出的是**正文**输入框——想到什么写什么，**标题不用起**：Engram 在后台调模型读完这段正文替你拟一个**精简**的短标题（12 字以内；没接模型 / 调用失败就退化成按正文首句取的规则标题，灵感照记不误），落 `原始资料/灵感碎片/`（文件名沿用 `YYYY.MM.DD_标题.md`）并进编辑器接着写。提交时按钮转成「正在拟标题…」，失败原因显示在框里、正文不丢可重试；`Ctrl/⌘ + Enter` 提交，`Esc` 取消。新建 Wiki 页面走侧栏顶部的「+」。首页快捷卡片同时给出「记一条灵感」与「新建页面」两个入口。
- **正文不重复一级标题**：原始资料的标题由文件名（`YYYY.MM.DD_标题.md`）与 frontmatter `标题` 承载，页头显示的就是它，所以正文里不再写一遍 `# 标题`——新建文件、记灵感、`save_chat`、收集箱入库、`create_raw_material` 都会去掉正文开头的一级标题（正文中后段的一级标题属于原文档结构，原样保留）。
- **历史文件不迁移**：二级分类上线前已在 `原始资料/` 根目录的资料仍在原地，侧栏「文档」分组把它们一并列出（磁盘不动、证据来源路径不变、无需重新提炼）；在编辑页选一次分类即可归位。
- **对 Agent 与检索无影响**：提炼清单（`list_raw_files` 递归）、证据门禁（按 `原始资料/` 前缀）、FTS 检索、同步、回收站都按路径处理，二级目录照常可被提炼、可作逐字证据来源。
- **Agent 规范同步**：`kb_guide`、MCP `instructions` 与入库纪律 skill 都写明三个二级目录与各自落点，Agent 不会自造目录。

### 🗃 收集箱（任意格式暂存 → 语义转换 → 入库）

图标栏「知识图谱」下方进入收集箱：**拖入任意格式，单文件大小不限**（流式写入，不设固定单文件上限），落盘即多端同步，同名不覆盖、自动加序号。也可粘贴公网 HTTP/HTTPS 网页链接，服务端抓取 HTML 并按网页标题保存为 `.html`；不执行脚本、不下载图片与样式，需登录或依赖脚本的页面可能无法完整保存。需要完整视觉版时可从浏览器另存为 HTML 或 PDF 后拖入。

- **不属于知识库**：不参与检索、不出现在目录树与原始资料清单、不被 Agent 当作事实依据或证据，通用文件接口（预览 / 取内容 / 导出）对它一律拒绝——**不支持内置浏览**。原文件在桌面端用系统默认应用打开，Docker / 浏览器端直接下载。
- **语义转换，不是格式搬运**：点「转为 Markdown」把原件**按内容语义重写**成人类可读的 Markdown——表格重建、按体裁重组信息（访谈按对象/现状/痛点/关注点）、数字与人名逐字保留、不补内容，文末固定「待确认」与「原件信息」。模型还会从成稿提炼核心内容，按转换当天的本地日期生成 `YYYY.MM.DD_核心内容.md`，产物留在 `收集箱/转换结果/` 供审阅（带来源 frontmatter），此时仍不进知识库。每份原件只保留最新一份转换产物：重转成功后删除该原件的旧产物，失败时保留旧产物；不同原件不会互相覆盖。Office / PDF / 文本由服务端直接转（长文按段落分块，超上限会明确标注只覆盖部分）；图片与音视频走 Agent 通道（视觉识别 / 转写）；压缩包等给出可读的说明而不是假装能转。
- **转换过程可见**：每次开始转换都会在内置 Agent 中新建独立对话，记录排队、读取、分段转换、产物路径或失败原因。收集箱会自动打开该对话，文件行上的「查看过程」可随时返回；批量转换的每份文件各有一条对话。转换期间对话自动刷新，完成后可继续提问。
- **两条通道、一份规范**：服务端一键转换与 Agent 转换共用 `inbox-semantic-to-md` skill，产物同位置同结构。外部 Agent 与内置 Agent 经 MCP `list_inbox` / `read_inbox_item` / `write_inbox_markdown` 读写收集箱；内置 Agent 空会话里会出现「转换收集箱里的内容」推荐项。
- **入库才是纳入知识库**：确认无误后点「入库」，产物以同一个 `YYYY.MM.DD_核心内容.md` 文件名直接复制进 `原始资料/文档/` 并登记为页面；同名自动加序号。从此可在原始资料清单中找到，也可检索、引用、作为逐字证据来源。旧版曾落入 `原始资料/收集箱/` 的文件会在启动时迁到 `原始资料/文档/`，保留页面 ID。原件与当前产物都留在收集箱，可随时再转一版；已入库的副本不会因重转而自动改动。

### ✨ 内置 Agent（聊天抽屉）

- **随包内置、开箱即用**：DeepSeek Harness（dsh）作为依赖随桌面端与 Docker 镜像一起发，无需另外安装；左栏点亮 ✨ 打开聊天抽屉，输入问题即可
- **空会话有常驻推荐项**：新会话的空白页给几条点一下就能问的问题，其中「**下周的工作任务有哪些**」是常驻项——点它，内置 Agent 会按**当天日期**算出「下周」的窗口，再从知识库里提炼待办：最近几周的日课三条复盘、双周工作复盘、专题会与客户会见纪要里的「待办事项」、行程安排、行动计划，以及客户 / 区域 / 团队实体页的时间线；收口按「客户与项目」「团队与例行」「时间待定或逾期」分节，每条写成 `- [ ] 事项 —— 责任人/时间（依据：原始资料/xxx.md 或《页面标题》）`，末了单列「资料缺口」；指不回原文的不列，没有依据就直说库里没写。收集箱里压着待整理文件时，「转换收集箱里的内容」会临时插到最前
- **选中文字就地提问**：在文件预览（txt / Markdown / HTML / PDF 与图片的提取文本 / Office 简化预览）、页面阅读视图与编辑器里选中任意文字，右击选「在 Agent 中提问」，聊天抽屉随即打开、光标已在输入框；选中的原文**逐条显示在输入框上方**（默认展开看全文，长文自身滚动，点标题可收起为一行摘要，每条可单独移除或一键全部清除），多条选中会累积成「片段 1 / 片段 2…」并各自带出处（文件路径或《页面标题》），随消息一起送给 Agent；同一菜单里的「在知识库中搜索」照旧
- **右侧悬浮卡片 / 满窗全屏**：非满窗档是一张**浮在右侧的毛玻璃卡片**——与左侧文件树同一套材质（圆角 8px、四周留 8px 露出底色、投影），正文让出它的宽度，聊天时不遮正文；打开与收起从右缘滑入滑出（轻微缩放 + 淡入，160ms，与左侧栏同一套节奏），正文的让位走同一段过渡跟着平移。卡片只做位移与淡入淡出、不参与重排，所以**对话再长（几百行文本）开合也不掉帧**；光标送进输入框时不带滚动，呼出时左侧图标栏与文件树纹丝不动。点标题栏的满窗按钮即在「悬浮卡片」与「满窗铺满内容区」之间切换（满窗只铺满正文区，左侧图标栏与文件树保持可见可用；桌面端标题栏与窗口按钮照常可用），选择记在本地，下次打开沿用；满窗下按 `Esc` 或点「收回右侧」即可还原
- **宽度可拖、拖过头自动满窗**：悬浮形态下拖卡片左缘就能调宽（最小 320px，悬浮宽度本身不设上限——**拖过窗口宽度 70% 即自动转「满窗」**，因为满窗把正文列居中限宽、比一条占满屏幕的卡片好读；「收回右侧」时回到原来的悬浮宽度）；双击手柄还原默认、聚焦手柄后 `←` / `→` 微调、`Home` / `End` 直达两端，宽度记在本地，下次打开沿用（浮层、满窗与手机端铺满可用宽度，不提供拖拽）
- **最小化：手动随时收 + 满窗切内容自动收**：标题栏有「最小化」按钮（—），任何形态（悬浮 / 满窗）点它都把抽屉收起来；满窗下点「设置」「实体页」「搜索」「图谱」或侧栏任意页面（双链跳转、搜索结果、返回轨迹同样算）也会自动**最小化**——不占右侧、正文立刻占满整屏，不会换一条卡片继续挡着新内容。最小化后由右下角**状态胶囊**接管：空闲时提示 4 秒「已最小化 · 点此继续对话」，有轮次在跑则常驻显示「回复中 + 当前动作 + 用时秒表」；点胶囊或图标栏 ✨ 即回到最小化前的形态，会话、草稿与宽度偏好全部保留（与「关闭」的区别只在它明确告诉你抽屉去了哪儿）
- **只读沙箱 + 仅经 MCP 工具**：Agent 的工作目录是数据目录下的空壳 `data/dsh/workspace`，不是知识库目录；知识库的读写全部经 `mcp__engram__*` 工具，因此 `write_page` 的证据逐字校验、两来源门禁与操作日志照常生效
- **单列转录、执行记录可展开**：对话流与 dsh 同构——用户消息、思考过程、Agent 正文、工具调用（检索 / 读页 / 写页…）按发生顺序排成一条单列流；工具调用默认收起为一行摘要（工具名 + 参数 + 状态），点开即看完整参数与完整结果（等宽滚动，不截断），失败自动展开；可把一轮对话「沉淀到原始资料」供后续提炼
- **正文按 Markdown 成块渲染**：助手正文、你发的消息与子代理产出（含生成中的流式片段）都按 Markdown 渲染，不再是一行行纯文本：标题按转录层级降一级成块，有序 / 无序列表（含嵌套与 `- [x]` 任务项）、表格（识别左中右对齐、超宽自动横向滚动）、引用块、分隔线、行内代码与围栏代码块（带语言标记）、`**粗体**` / `*斜体*` / `~~删除线~~` / 引用角标 `[S1]` 全都正常显示，观感与 Wiki 阅读页对齐；`[[页面]]` 与 `[标题](#wiki/…)` 渲染成**双链胶囊**，点它按标题查页并跳到阅读页（页面不存在时问一句要不要新建，跳转前先收起抽屉）；站外图片自动降级成链接（离线环境不出破图），危险协议的链接只显示文本，原文 HTML 一律转义；渲染器为流式输出做了针对性处理——代码块还没写到收尾围栏时仍按代码块渲染、段落内单个换行保留为换行、单条消息做渲染缓存
- **过程信息保持等宽原文**：思考过程、工具调用参数与结果、选中片段继续用等宽文本原样显示——这些是排查用的原始日志，渲染成 Markdown 反而看不清
- **运行中也能接着发消息（排队）**：一轮回复期间输入框照常可用，发出去的消息立刻进对话并标「排队中 · 当前这轮跑完自动接着回复」，当前这轮一收口就自动接着回复它（同一会话仍是一轮一轮跑，先来先服务；想一次说清就 Shift+Enter 换行写完再发）；排队条数在标题胶囊与贴底状态条上都标着
- **「停止」按下即停**：停止按钮挂在抽屉标题栏「回复中」胶囊右侧（输入框右下角固定留给「发送」，所以运行中也能发消息），按下**立刻**停住当前动作——服务端先把这一轮落成终态并广播，dsh 运行时在后台回收，不必等进程退出（否则界面会挂着「正在回复」好几秒）；同时**排队中的消息一并撤下，原文退回输入框**（它们还没真正发出去，改完可以再发），该轮留下的正文与思考段照旧保留，可「重试」
- **子代理派活看得见**：Agent 把活派给子代理（dsh 的 `subagent` / `subagent_fork` / `workflow` / `ralph` 子会话）时，对话流里会出现一颗**子代理胶囊**（默认收起，不占屏）——一行装下标签（模型给的 3-5 词任务名）、当前动态、运行状态与用时秒表，运行中 / 后台的胶囊带一圈低强度呼吸光晕，失败 / 完成各自换色；点开在胶囊下方接一块**详情面板**：**委托任务原文**、子代理自己的**过程**（它调了哪些工具、参数摘要、成败与时间点）、以及**产出**（最后一条正文；收工时以 dsh 的 `subagent.finished` 定稿）；子代理再派子代理会以同款小胶囊嵌在父卡里。派它的那次工具调用并进同一张卡，不会重复两行；本轮结束时仍在后台跑的子代理标「后台运行中」。为此内置 Agent 的约定里写明：委派子代理默认同步等结果（`run_in_background: false`），除非用户明确要并行后台作业
- **只要在跑就一直显示**：一轮回复期间，抽屉标题右侧常驻「回复中 + 用时」，转录区底部有一条**贴底的运行状态条**（当前在干什么：子代理 / 工具 / 步骤，逐秒跳的用时），滚到哪一段都看得见，收口即消失；抽屉最小化后由右下角**常驻状态胶囊**接手（回复中 + 当前动作 + 用时秒表，点它回到对话），页面 / 设置 / 搜索 / 图谱任何界面都看得见；抽屉关着（或页面刷新后）时图标栏的 ✨ 会有呼吸动效 + 脉冲点，跑完换成「新回复」小红点——任何时候都知道它还在不在干活
- **提问弹在对话最下侧，点选即答**：Agent 需要你拍板时（既定场景是公司工商全名核验：是否允许联网查企查查 / 天眼查、是否改用全名），问题会以**选项卡**的形式贴在转录区底部（在跑状态条上方，滚到哪都看得见）：单选点一下即答复，多选或想自己说就点「提交」（也可以在输入框补一句你的口径）。答复后 Agent **在同一轮里拿到结果继续干活**——不用切页面、不用等下一轮；抽屉关着时有 toast 提示，等太久没答就作废、Agent 改在正文里问你，不会把一轮对话挂死
- **思考过程流式显示**：模型的推理内容不再丢弃，单独成块（弱化虚线框 + 灯泡图标），默认展开、边生成边长出来；dsh 的事件是「按步提交」的，所以思考过程按会话事件里保留的模型原始增量与真实节奏回放（单步封顶 2 秒，正文等这一步回放完再显示，避免思考与答案错序）；这一段跑完就自动收起为一行（`思考 N 秒`，不等整轮结束），点标题可随时展开看全文，手动点过就以手动为准；思考段只进对话流，不进模型上下文，也不随「沉淀对话」写进知识库
- **多对话并行**：头部「会话列表」按钮打开列表（替换原来的窄下拉框），标题、相对时间、进行中状态一眼看全，可搜索、重命名、删除；每个会话各跑各的一轮（同会话仍单飞），切走不中断，后台会话跑完在列表里标「新回复」、图标栏亮小红点，切回来即可接着看
- **标题按内容自动更名**：会话还没起名时，发第一条消息立刻用消息本身取一个临时标题；首轮跑完再用模型按整段对话总结一次（走内置 Agent 同一条模型路由，失败就保留临时标题）；列表里标「自动命名」；手动改名后自动命名不再覆盖（改回自动命名目前只能新建会话）
- **自己的 Key、自己的账**：模型与 Key 填在 设置 → Agent 接入 → 内置 Agent（Key 仅存本机数据库，运行时经环境变量注入，不写进配置文件的明文里）；不填就不能对话，Engram 其余功能完全不受影响
- **API 地址可自定义**：地址留空走 DeepSeek 官方（`api.deepseek.com`）；填中转站或自建网关（`openai-completions` / `openai-responses` / `anthropic-messages` 三种协议）即改用该地址与它自己的模型名——地址与模型清单写进内置 dsh 的 `data/dsh/settings.yaml`（dsh 的 `llm-pi-ai` 自定义 provider 路由，不含 Key），改完下一次对话即生效（池里的旧运行时按旧路由跑，会自动重开）；自动更名的总结请求也走同一条路由
- **缓存命中看得见**：一轮回复收口时，对话流里标出**这一轮的缓存命中率与整段会话的累计命中率**（如「本轮缓存命中 70% · 会话累计 63%」），鼠标悬停给细账（未命中输入 / 缓存读取 / 输出 tokens + 本轮请求步数）；跑动中贴底的运行状态条也实时显示「本轮命中 N%」，不用等收口。命中率的分母是这一轮实际发给模型的 prompt tokens（**含命中的那部分**，不是 input tokens）；**子代理的用量单独一行标注**（虚线胶囊「子代理缓存命中 N%」），不混进本轮主账；模型路由没上报缓存字段（部分中转站）或整轮确实没有缓存数据时**整块不显示**，不拿 0% 充数。数据来自 dsh 事件里的 usage，服务端按轮累计后随快照下发（`assistant_runs.usage`），刷新页面、切走再回来都在
- **会话与桌面/Docker 一致**：会话、消息、思考段、标题来源存本机库（`assistant_*` 表，`assistant_sessions.title_source` 区分默认 / 自动 / 手动命名），dsh 自身的会话日志随 `data/dsh/` 走持久卷，续聊不丢上下文

### 📋 任务看板（侧栏一键生成）

图标栏「任务看板」进入一页看板：**点进去就自动让内置 Agent 从知识库提炼下周要干的活**，不用先开聊天抽屉、也不用自己写问题——提问文案与作业手册都是现成的（与空会话里那条「下周的工作任务有哪些」同一套取数口径），Agent 在后台跑，页面只显示「正在提炼 + 当前动作 + 用时」，你可以随时切走。

- **答复直接变成看板**：Agent 除了给人看的正文，还会按固定字段再给一份机器可读清单（`summary` / `groups[].cards[]` 的 `text·owner·when·date·kind·repeat·customer·team·source` / `gaps`），页面把它渲染成卡片墙；每张卡带责任人、时间、客户（或「X 组内部工作」）、端组与依据，依据能落到原文的（`原始资料/xxx.md`）给一个可点的入口，直接跳到文件或对应页面。手册里给每列压了张数上限（最多 8 张，按「时间明确 > 影响大」取舍，同类合并），看板一眼扫得完；模型忘了给清单时退回按正文分节解析（日期从「9/30 前」这类文字里抠），两样都解析不出来才显示空状态（不给半张看板）
- **按天 / 分列两个视图（共用一套筛选）**：**按天**（默认）就是「1、2、3…每天要干什么」——**周一到周五依次排最前**（没有安排的显示「本日暂无」，压成一行不占地方），周六周日跟在工作周后面，最后才是「周期 · 例行」（日课三条、每周一交周重点、每月碰头这类周期事项，不铺满每一天）、「已逾期」与「下周之外 · 待定」（只有月份、或压根没时间）；**分列**按来源三节排（客户与项目 / 团队与例行 / 时间待定），**逾期单独一列**（从各节里抽出来，不再重复出现，卡的日期早于今天就算逾期并标「逾期 N 天」）
- **每一块都能收起**：点标题整条即收起/展开（箭头指方向，右侧始终留着条数），工具条有「全部收起 / 全部展开」——10 多块时先收起来当目录看。收起状态**按星期几/固定块记在本机**（周一到周日各记一条），换一周日期变了偏好还在
- **三档筛选**：人物（一张卡写多个责任人会逐个拆开）、客户（没有客户的内部工作按端组归到「北京组内部工作 / 天津组内部工作 / 大区内部工作」）、端组（北京组 / 天津组 / 大区，由 Agent 按材料填）。每一档都是可点的小胶囊、带条数，同一档内多选是「或」、档之间是「且」；顶部实时显示「共 N 条 · 筛后 M 条」，一键清除。视图选择记在本机，下次进来沿用
- **缓存 + 手动刷新**：进页面有 6 小时内的结果就直接看（标题旁标出生成了多久）；过期、没有、或遇到旧版契约（v1 没有日期/客户/端组）才自动重跑，右上角「刷新」随时重新问一遍（约 1-3 分钟，跑动中复用同一轮，不重复烧 token）
- **跑完自动换版**：提炼期间旧看板照常显示不挡着，跑完自动换成新的；最近一轮失败时保留上一版并说明失败原因，可「再试一次」。结果落在专用会话「任务看板」里（同一条问答走的是内置 Agent 的排队、停止、事件流与用量记账），在聊天抽屉里能翻到过程；「复制清单」把**当前视图与筛选下的**看板导成 Markdown（逾期/每天/待定各一节），贴进周报或群里都能看
- **不写知识库**：整页只读；文末单列「资料缺口」——库里没记录、要你自己补的环节（比如某一周的日课复盘缺失），补进原始资料后下次看板就更准

### 🧠 面向 Agent 的 MCP 接口（26 工具）

在 设置 → Agent 接入 →「接入目标」选择目标并生成 Token（`Authorization: Bearer`，MCP/CLI/REST 三用），streamable HTTP 端点 `/mcp`：

| 工具 | 说明 |
|---|---|
| `search` | 关键词检索（Wiki 页面 + 原始资料提取文本，FTS5 BM25）；中文 bigram 分词 + 错字兜底 + 同义词扩展 |
| `list_pages` / `read_page` | 目录树（页面带提炼规则版本；`outdated=true` 只列落后页面，`path`/`tag` 按路径前缀与标签过滤） / 按标题或 ID 读页面全文 |
| `related_pages` | 读页面图谱关联（入链/出链邻居与实体关系，与编辑器「相关页面」同一数据） |
| `page_evidence` | 读页面证据账本（来源、版本、事实与逐字引文） |
| `list_raw_files` / `read_raw_file` | 原始资料清单（含提取状态与「已提炼」标记；`pending=true` 只列未提炼文件）与读取；图片/PDF 返回原图（image 内容）供视觉 Agent 自行识别 |
| `create_raw_material` | 用户明确要求保存调研结果时新建 Markdown 来源文件：调研成果落 `原始资料/文档/`、随口记的零散内容落 `原始资料/灵感碎片/`；目标已存在即拒绝，不覆盖；聊天记录仍用 `save_chat` |
| `read_page_asset` | 读页面/资料正文里引用的图片原图（传正文里的 `/media/<父项id>/<文件名>` 引用，图片以 image 内容返回） |
| `list_inbox` / `read_inbox_item` | 收集箱清单与读取：用户拖进来的待整理原件（图片以 image 内容返回）。**收集箱不属于知识库**，这些内容不得作为事实依据或证据 |
| `write_inbox_markdown` | 把原件的语义转换结果写成 Markdown，落到 `收集箱/转换结果/`（仍不进知识库；入库由用户在界面上确认） |
| `write_page` | 写页面（只允许 `Wiki/` 下）；新建概念/实体页必须带 `evidence` 过两来源门禁，引文服务端逐字校验 |
| `rename_page` | 重命名页面：文件随标题移动、`[[旧标题]]` 双链自动重定向，页面 ID 与图谱边保持不变 |
| `move_page` | 移动页面到 `Wiki/` 树内其他目录（页面 ID 与图谱边保持不变，可顺带改标题） |
| `delete_page` | 单页软删除入回收站（可恢复，按标题 / ID / 路径定位）；只允许 `Wiki/` 下的页面，`原始资料/` 与 `AIWorks/` 拒删，无永久删除/清空回收站能力 |
| `save_chat` | 与 Agent 的聊天记录沉积到 `原始资料/对话/`（原始资料的二级目录之一；**须用户明确指示**才可调用；不用于保存调研报告） |
| `entity_name_check` | 公司全名核验：名称不是工商全名、资料库里也查不到时登记——**全库唯一允许问用户的事**，但问在对话里（服务端先在页面标题 / 证据账本 / 原始资料 / 正文写明全名的提法里自查，有全名就直接返回、不问用户；正文里带「待核实/候选」标记的写法只算未核实候选） |
| `entity_name_answer` | 回填用户在对话里给出的核验答复（`allow` / `deny`）：允许联网查询，或同意改用全名（同意即**由服务端改名**） |
| `entity_name_propose` | 回填 Agent 联网查到的工商全名（企查查 / 天眼查），随后再问一次是否改用全名（保持页面 ID、双链重定向、自动记日志）；查不到就不传 `fullName` |
| `ask_user` | **内置 Agent 专用**：在 Engram 对话最下侧弹出选项问用户并等他点选，点选后当场返回答复（外部 Agent 用自带提问能力，不要用它） |
| `list_entity_names` / `entity_name_audit` | 读核验清单（已登记还没回填答复 / 未办结 / **最终不是全名**的条目） / 全库公司页名称盘点（标题不是工商全名形态的页面） |
| `kb_guide` | 下发《Agent 作业指南》全文 |
| `skill_list` / `skill_guide` | 内置作业 skill：先列清单（名称 / 用途 / 何时用 / 版本），需要时再取某份全文。skill 与指南同级但按需获取，版本独立于 `GUIDE_VERSION`，改 skill 不触发全库「规则落后」 |

**各 Agent 接入**：设置 → Agent 接入 →「接入目标」选 ZCode 桌面端 / Codex CLI / DeepSeek Harness（dsh）/ WorkBuddy / Qoder 可在本机一键注册；Kimi Work 可一键登记个人插件，然后在 Kimi Work 插件中心点击安装。各目标仍提供专属配置片段供跨设备手动接入；「其他 Agent」可复制 Codex / Claude Code / Kimi Code CLI / 通用片段。手动接入区显示 MCP Server 地址、Token 管理，可选择具体 Token 生成片段；面板底部「查看工具」逐条列出现有 MCP 工具（功能、参数、要点与 CLI 等价命令）。

| 一键接入目标 | 写入位置 | 说明 |
|---|---|---|
| ZCode 桌面端 | `~/.zcode/cli/config.json` | 检测本机安装与登录状态，注册/移除知识库 MCP |
| Codex CLI | `~/.codex/config.toml`（`[mcp_servers.engram]`） | 只维护 Engram 这一个 TOML 表，其余配置、注释与其他 `[mcp_servers.*]` 条目原样保留；重启 Codex 会话生效 |
| DeepSeek Harness（dsh） | `$DSH_HOME/cordis.patch.yml` | 对整个 patch 层注册，对所有 dsh profile 生效 |
| WorkBuddy | `~/.workbuddy/mcp.json`（海外版 `~/.workbuddy-ai/mcp.json`） | 按客户端自身规则解析配置目录（`WORKBUDDY_CONFIG_DIR` / `CODEBUDDY_CONFIG_DIR` 优先），检测到的变体目录都会写上；只维护 `mcpServers.engram`；新写入的服务在连接器页是「待信任」，需确认启用一次 |
| Qoder | `~/.qoder/settings.json`（国内版 `~/.qoder-cn/settings.json`） | 国际版与国内版目录都检测、都注册（`QODER_CONFIG_DIR` / `QODERCN_CONFIG_DIR` 优先）；只维护 `mcpServers.engram`；重新打开会话后使用；QoderWork 仍需手动导入 |
| Kimi Work（Windows） | 个人插件市场 | 调用客户端自带命令登记插件，随后在 Kimi Work 中安装 |

一键注册只对本机安装的客户端落地（需与 Engram 桌面版同一台电脑）；Docker/远程部署时让 Agent 用「其他 Agent」的 MCP 片段或 `engram login` 连接。Claude Code 示例：

[WorkBuddy](https://www.workbuddy.cn/docs/workbuddy/Changelog) 一键注册写入用户级自定义 MCP 配置（国内版 `~/.workbuddy`、海外版 `~/.workbuddy-ai`，按客户端自身规则解析并全部登记）；[Qoder](https://docs.qoder.com/zh/qoder/connectors) 一键注册同时写入国际版 `~/.qoder/settings.json` 与国内版 `~/.qoder-cn/settings.json` 的 `mcpServers.engram`；[Kimi Work](https://www.kimi.com/help/plugins-and-skills/create) 一键登记使用客户端自带的 `kimi-plugin register-personal`，安装仍需用户在「插件 → 个人」完成。QoderWork 与其他远程客户端可手动导入对应片段。客户端须能访问片段中的 `/mcp` 地址。

```bash
claude mcp add --transport http engram http://<主机IP>:18080/mcp \
  --header "Authorization: Bearer <你的token>"
```

### ⌨️ engram CLI（零依赖，Node 22）

`node server/dist/cli/cli.js <command>`（Docker 内 `docker exec engram node dist/cli/cli.js`；桌面端 `ELECTRON_RUN_AS_NODE=1 Engram.exe app.asar/server/dist/cli/cli.js`）。**本机服务零配置**：服务端启动时自动把本机地址与专用 token 登记到 `~/.engram/config.json`（地址随实际端口自适应，不写死；用户手动 `login` 保存的配置优先、不被覆盖），CLI 开箱即用，远程服务再手动 `login` 一次。命令覆盖 `login / status / import / files list|read / search / pages list|read|write|delete|evidence / names check|propose|list|audit|answer / chat save / guide / mcp-config`，全部支持 `--json` 供 Agent 消费；`pages read/evidence/delete` 接受 `pages list` 返回的页面 ID（UUID）、标题或页面路径；`pages delete` 只把 `Wiki/` 下的页面移入回收站（软删除，`原始资料/`、`AIWorks/` 拒删）；`names check|propose|list|audit` 是公司全名核验通道（登记请示 / 回填联网查到的全名 / 读清单 / 全库公司页盘点），`names answer --allow|--deny` 供用户在终端答复；`files list --pending` 只列未提炼文件（提炼作业索引用）；`pages list --outdated` 只列提炼规则版本落后于当前指南的概念/实体页（规则升级后重提炼用）；私网/环回目标经 `login` 显式登记后放行（出网校验协议/云元数据阻断/DNS rebinding 防护）。

### 📄 页面编辑与管理

- **Markdown 编辑器**（Vditor）：即时渲染 / 分屏切换、大纲、全屏、`Ctrl+S`、中文输入法组字保护；图片粘贴/拖入自动存成**当前页面的私有资产**（从 Word 复制的 base64 内联图也会落盘）；扁平化编辑页（灰底正文直接铺满，顶栏与状态栏是**悬浮毛玻璃胶囊**，与工具栏卡片同一套语言，不占文档流）——顶栏是 `Wiki / 分区 / 标题` 面包屑 + 常驻保存态（绿点 + 文案，有未保存改动时点击即保存，Ctrl+S 不变）+ 按文件的「自动保存」开关（默认开）+ **手动保存按钮**（关闭自动保存才出现，无改动置灰、有改动点亮）+ 「正文宽度」百分比按钮；标题 30px，文档列宽按**正文可用区的百分比**（默认 70%，与阅读视图共用同一份偏好，改一处两处都变），页头/正文/工具栏左缘始终对齐；类型与标签是圆角 pill（类型软色强调、`+ 标签` 虚线按钮点开输入框）；工具栏是 fit-content 白卡片，按「格式 / 段落 / 插入 / 历史」四段分组（双链、源码切换、全屏、大纲、沉浸阅读收在末段）；左下悬浮状态栏（字数 / 模式 / 来源 / 图谱 / **本页关联**，手机端抬到底部导航之上）；深色模式下滚动条随主题变色（原生下拉已全部换为自绘控件，见「界面」一节）
- **图片＝内容的私有资产**：图片不是独立资料，而是**某个 Markdown 内容（Wiki 页面或原始资料 md）的资产**——不会出现在左侧目录树、知识图谱、搜索结果或 AIWorks 索引里。唯一入口是**右击那个文件/页面 →「查看引用图片」**（带张数徽标，没有图片时置灰），打开「图片资产」**悬浮卡片**（与内置 Agent 卡片同一套玻璃材质，四周留边浮在正文之上、不压暗整屏，正文照常可见可点，`Esc` 或 ✕ 关闭）：缩略图墙、正文引用 / 未引用筛选、预览 / 另存 / 删除，底部标明归属。图片只能**插进某个内容里**：编辑器内粘贴/拖入，或从资源管理器把图**拖到侧栏的某个页面/Markdown 资料上**（行高亮，引用自动追加到正文末尾）；把图片拖到「原始资料」分区空白处会被拦下并提示拖到具体条目（二级分组同理：图片永远属于某个 md 条目，不属于某个分类）。正文里存的是根相对路径 `![图注](/media/<父项id>/<文件名>)`——页面在 Wiki 各子目录之间移动不会失效，下载/导出的 md 也可移植：**下载一份带图的内容时图片一起打包**（md 放包根 + `assets/<父项id>/`，正文里的 `/media/` 自动重写成相对路径，解压即见图；侧栏菜单直接标出「含 N 张图」，没有图片的文件仍按原样单文件下载），批量导出同样带图并按 md 在包内的层数退回相对层级。**外链图片在保存时自动本地化**：远程图片抓成本地资产、原 URL 记进图片 title 作为出处（离线可读、不怕图床挂掉、不把阅读行为泄露给第三方），抓不到就保留外链原样下次再试；抓取带 SSRF 防护（私网/环回/云元数据端点阻断）与体积上限。
- **全屏编辑**：点工具栏最右侧的全屏按钮，页头与状态栏让位、编辑区铺满正文区（左侧图标栏与文件树保持可见可用，桌面端标题栏拖拽条不受影响），顶栏保留保存态与「正文宽度」；按 `Esc` 或再点一次按钮退出；进入阅读视图或换页自动退出全屏
- **`[[双链]]`**：输入 `[[` 或工具栏按钮唤起补全弹窗（跟随光标、↑↓ 选择、回车插入），编辑态虚线下划线标识，点击跳转；改名自动重定向全部引用
- **改标题＝改名**：在编辑器顶部标题框改完名字，磁盘文件名与侧栏目录里的条目同步跟着改（其他页面里的 `[[旧标题]]` 双链一并重定向，页面 ID、图谱边、证据账本不变）；正文 H1 属于正文，不会被服务端改写。反向同样成立：外部 Agent 或用户在软件之外直接给文件改名/移动，索引会把这一行认领到新路径（标题随新文件名，ID 不变，双链重定向），不会留下幽灵页或重复行
- **带外改动实时可见**：外置 Agent 或用户绕过界面直接往数据目录的 `原始资料`/`Wiki` 写文件、改名、删除，服务端文件监听会在约一秒内对账（登记索引、排上文本提取）并推给所有打开的界面——侧栏目录立即出现、md 点开就是编辑器，不用重启；监听不可用的平台（如部分移动端运行时）自动退回「启动扫描补齐」的旧行为，不影响其他功能
- **页面类型系统**：概念 / 实体五子类（人物/客户/组织/项目/其他），类型即目录，头部下拉切换自动移动
- **来源证据抽屉**：每页的原子事实 + 原文逐字引文 + 来源版本时间线，AI 写的内容可溯源、可核对
- **回收站**（软删除，Agent 经 MCP `delete_page` / CLI `pages delete` 删除同样只入回收站）、标签、沉浸阅读（**打开页面默认进入阅读视图**，需编辑时点「返回编辑」；正文字号 1px 连续可调，8–48px，点字号可直接下拉选择；**正文宽度按可用区百分比选档**（50%–100%，默认 70%），点工具栏的百分比按钮即选；标题左侧箭头可按节收放（H2–H4，收起时连同子节一起折叠，状态仅当前会话）；**本页目录开关在顶栏一级**（图标+「目录」，宽屏展开/收起右侧常驻目录列、窄屏把目录摊到正文上方，本页没有小标题时置灰）；底部悬浮胶囊显示「字数 · 阅读时长」）
- **本页关联**（双链邻居 / 实体关系）：入口是编辑视图左下状态栏和阅读视图底部胶囊里的「关联 N」（没有关联时不出现），点开从下侧弹出面板——双链分「→ 本页引用 / ← 引用了本页」，实体是 chip，点条目跳转并记入「返回上一页」，面板里还有「图谱 ↗」直达页面图谱；`Esc` 或点面板外关闭（阅读视图里 `Esc` 只关面板，不会退出阅读）；手机端弹窗改成贴屏幕底部的下侧弹窗（带遮罩，点遮罩关闭）；正文尾部不再占一行「本页关联」折叠摘要
- **双链往返**：点双链/本页关联跳转后，阅读工具栏与编辑顶栏出现「返回上一页」（Alt+←），可沿跳转链路逐级回退；鼠标扫过该按钮即下拉列出链路上所有可返回的页面名称（栈顶在最前，标注「上一页 / 第 N 层」），点任意一层直接跳回，其余记录仍可继续逐层返回；从侧栏、搜索等处跳转会清空该链路
- **中文搜索增强**：全文索引按 bigram 分词（单字也能命中）；**错字兜底**——词表内做编辑距离 ≤1 的邻居扩展（「部属」→「部署」）；**同义词扩展**——设置 → 数据与存储 → 搜索同义词（默认折叠，内置常用词表），每行一组逗号分隔，查询命中组内任一词即一并检索

### 🕸 知识图谱

全局图 / 单页局部图（自研 canvas 力导向渲染），按类型着色、死链红色节点；保存页面自动提取 `[[双链]]` 建边（零成本、零模型）。悬停节点即高亮**自身 + 一跳关联节点 + 全部相连连线**（强调色描边 + 加粗连线），非关联节点与连线保持原样、不压暗变灰；节点为扁平实心/描边绘制，不带泛光。

### 📎 附件与在线预览

- **ONLYOFFICE 在线协同编辑**（Docker 版，JWT 鉴权），docx/xlsx/pptx 浏览器编辑保存，带版本快照回滚
- **PDF 查看器**：缩略图/搜索/跳页；「原文 / 提取文本」双视图
- **图片查看器**：缩放旋转拖动（页面图片资产的预览走同一套查看器）
- 文档文本层自动进入检索索引；图片与无文字层的扫描 PDF 不做内置 OCR——外部 Agent 用 `read_raw_file` / `read_page_asset` 读原图自行识别
- **图片资产清理**（设置 → 数据与存储 → 图片资产）：列出**未归属**（历史遗留的散图，用户已不能单独上传图片）与**未被引用**（正文里已删掉引用的残留）两类，可逐张删除或全部清理；未归属图片还能选一个页面「挂载」，服务端把它移到该页面名下并把引用补进正文末尾
- 索引、关系图更新与文档解析作为系统维护在后台自动完成，不显示通用任务队列；耗时的文档解析进度只在对应文件旁就地展示

### 📱 多端与部署

- **多端实时同步（同步群组）**：把多台设备组成一个同步群组——选一台做**中枢**（建议常开的 NAS Docker 版，唯一需要可被访问的设备），其他设备用中枢签发的绑定令牌加入。页面、附件图片、原始资料文件与证据账本**秒级双向同步**（含「已提炼」标记：账本随全量对账按来源路径补齐，后加入或离线较久的设备不会停在「A 端已提炼、B 端仍显示未提炼」），字符级三方合并（非重叠编辑自动融合；无法融合的冲突按**修改时间最新者胜**：普通页面中被取代的旧版本以「原名-时间」重命名保留在原目录供人工核对后删除，AI 工作区直接以最新为准覆盖、不产生任何新文件）。每端都是完整本地库：**离线照常工作**；Android 本地优先版把 Agent 会话窄代理给已绑定的 Docker 中枢，因此手机退到后台后长任务仍在 NAS 上继续，模型 Key 与 Agent 配置不会下发到手机。各端 Agent 各连本地 MCP 互不影响，重连自动补同步（设置 → 连接与同步 → 多端同步；成员端侧栏「知识库」标题行右侧有**一键同步**按钮，点一下立即触发全量对账并直接提示完成/失败；**首页（欢迎页）在配置了同步后会常驻一条状态**——「同步中…（首次同步 / 正在全量对账 / 待推送 N 项）」「同步已完成 · 最近同步 X 分钟前」「同步中断，正在自动重连」「同步已停用」，中枢端显示「中枢运行中 · N/M 个成员在线」，点一下直达 设置 → 连接与同步 → 多端同步）。同步链路带**自愈能力**：远端变更应用失败自动断流重放（水位只前进不越过失败项）、文件拉取失败进待补拉队列周期重试、推送失败指数退避重试、每 15 分钟周期全量对账兜底；排查「同步不动了」用**同步详情**——设置 → 连接与同步 → 多端同步 只留一行状态摘要（连接/待推送/待补拉/最近同步）和「查看同步详情」按钮，点开的是一张与内置 Agent 同形态的**独立悬浮卡片**（可一键铺满窗口）：按级别（信息/警告/错误）、视角（中枢/成员/配置）、事件类型与关键词筛选，向前翻页回看，可导出 JSON / Markdown 给他人定位，也可清空。**每条记录都写清「哪个文件 + 做了什么增量」**，不是「推送 2 项变更」这种看不出所以然的汇总：`成员「客厅 NAS」修改页面「会议纪要」（+3 −1 行，1.2 KB → 1.3 KB）`、`成员「客厅 NAS」新增文件「原始资料/演示附件.bin」（1.2 MB）`、`成员「客厅 NAS」删除页面「旧稿」（删除前 142 B）`、`改名「旧名」→「新名」（旧路径 → 新路径）`、`本机修改页面「周报」（+1 行），已广播给 1/2 台成员`；冲突写清裁决依据与旧版本另存路径（`按修改时间以推送方（较新）为准，另一版本另存为「…」`）。**断联与故障也逐条记**：`与中枢的联系中断（已持续 14.7 秒，第 4 次重试）：fetch failed；8 秒后自动重连`、`已重新连上中枢，共断开 22.7 秒、重试 4 次；期间本机 2 项改动已排队，正在补推`、`「X」没能推送到中枢：…；3 秒后自动重试（队列还有 2 项）`、`全量对账失败：…；本端内容保持原样，联网后会自动重试`。日志落盘在 `data/logs/sync.jsonl`（保留最近 2000 条 / 7 天，重启后仍能回看）：成员端记推送、拉取、应用、断线重连与对账进度（对账摘要带示例文件名），**中枢端也会记**成员上下线、收到哪台设备的哪个文件、本机改动广播给了几台成员、冲突如何裁决、成员令牌重置与角色变更；AIWorks/ 下的系统页由应用自身高频改写，不进用户记录
- **密文统一掩码**：软件内所有密码、令牌、密钥（登录密码、修改密码、同步绑定令牌、成员令牌、MCP/CLI 令牌、Cloudflare API Token、更新源密码、内置 Agent API Key 等）默认显示 `***`，点击眼睛/文本即可查看完整明文，再点恢复掩码；更新源密码与内置 Agent API Key 展开后可直接复制
- **NAS Docker**：单容器 + ONLYOFFICE；**Android APP** 内置本地知识库与成员同步（断网可编辑）；**Windows 桌面端**（Electron 内嵌本地后端，托盘驻留、**开机自启（静默进托盘）**、自动更新）
- **IPv6 直连优先 + Cloudflare 隧道兜底**、内置 HTTPS（ACME/DNS-01）、内置 DDNS（Cloudflare API）
- **应用内自更新**：更新入口（桌面端 = 标题栏 `Engram` 右侧只写「更新」的小按钮，鼠标扫到即展开面板、点一下也能开合；Docker/浏览器端 = 右上角更新提示条）的主按钮一键直接更新（源码模式增量拉取重建并重启 / 桌面安装包下载后静默安装 / Docker 拉镜像重建后自动刷新页面，进度与失败原因就地显示，不再跳设置页）；自动检查走两级探测 + 自适应退避（启动 3～4 秒首查：网页 3 秒、桌面 4 秒，连续没发现更新时 2→5→10→30→60 分钟逐级拉长，有更新待处理时每分钟复查；发现更新或切回前台/唤醒立刻回到最短间隔）；**同步成员端可远程更新中枢**——桌面端绑定多端同步后，设置 → 连接与同步 → 服务器更新直接对所连服务器检查/拉镜像/重建容器（SSE 进度 + 代等恢复 + 失败自动回滚），无需登录服务器网页，同步连接即授权（更新源凭据读写仍仅服务器 owner）
- 设置页 4 大类：账户与外观、连接与同步、Agent 接入、数据与存储；**大类即整页**——每个大类一页平铺展示全部分组，分组是带头部色带（图标 + 标题 + 说明 + 状态徽标）的独立卡片，组间边界清晰；**每张卡片都可折叠**（点色带箭头或空白处收起/展开，折叠状态按分组记忆，锚点跳转自动展开目标分组）；软件更新拆为**服务器更新 / 桌面端更新 / 桌面端应用 / 更新源配置**四张独立卡片（服务器卡管版本与远程更新中枢，桌面端更新卡管安装包/源码版本，**桌面端应用卡管开机自启与桌面快捷方式重建**，更新源配置为两端共用的远端仓库地址与凭据）；**卸载 Engram 归入「数据与存储 → 危险操作」**，与清库 / 清日志并列（都是不可撤销的本机动作）；左侧二级锚点只做滚动定位与反向高亮（可跨大类点击，滚到底自动高亮最后一组），「危险操作」以红色分组收尾；窄屏下大类切换为两行胶囊导航；**侧栏拉宽挤压内容列时**，设置行自动转单列、控件换行，任何宽度下都不会被裁切

### 🎨 界面

- **下拉框全面自绘（2026-09）**：全站原生 `<select>` 退役，统一换成自研控件 `main/web/src/components/ui/AppSelect.vue`（8 处：编辑器**页面类型**，设置页**接口协议 / 接入目标 / MCP Agent 类型 / MCP Token / DDNS 域名 / 更新通道**，图片资产**「挂载到…」**）——弹层 `Teleport` 到 `body`，不再被滚动容器裁切或压住；空间不足自动**向上翻转**，四周留 8px 安全区并随视口收窄；支持方向键 / Home / End / Enter / Esc，`combobox + listbox` ARIA 完整。自绘菜单一并统一到设计令牌（圆角、内边距、hover 底色、层级 `--z-menu`）：侧栏排序菜单、编辑器正文宽度菜单、阅读视图字号/宽度/显示菜单、返回轨迹菜单、右键菜单、收集箱行菜单；同时修掉三处交互缺陷——收集箱行菜单**滚到底点开不再上弹**（`v-for` 内模板 ref 实际是数组，导致翻转测量提前 return）、返回轨迹菜单**键盘打开后关不掉**（补焦点移出 / Esc / 外部点击 / 滚动关闭）、编辑器宽度菜单 `aria-expanded` 失同步
- **UI 2.0（2026-09）**：暖灰中性色 + 统一品牌蓝（浅 `#0f6cbd` / 深 `#5aa9e6`）；圆角放大（控件 6px / 卡片 11px），输入框与按钮聚焦改为柔和光圈；**欢迎页**改为时间问候 + 库统计 + 快捷入口卡（新建/搜索/图谱/问 Agent）+ 最近编辑列表；**搜索页**重做命令面板式（48px 搜索盒、范围筛选 chips、结果卡片带类型徽标与关键词高亮、显示耗时）；**设置页**分组改为带头部色带的常开卡片（2026-09 末起大类整页平铺、二级锚点仅做定位）+ 主题切换改分段控件；**登录页**品牌渐变卡；**编辑器**改扁平纸面（灰底正文铺满、文档列 720px 正文 640px、标题 30px、`Wiki / 分区 / 标题` 面包屑、悬浮工具栏卡片；顶栏与状态栏随后也改成悬浮毛玻璃胶囊，并补回手动保存按钮），阅读视图顶栏 9 控件收为 5 个（行距/编号/目录收进「显示」菜单；2026-09 末起「本页目录」又提回顶栏一级开关——读者翻二级菜单找不到它，「显示」菜单只留行距/编号）
- **统一品牌图标（2026-09）**：品牌图形（知识核 + 倾斜轨道环 + 轨道电子）收敛为**生成器单一来源** `main/scripts/gen-brand-icons.cjs`——一处几何与配色推导出全部产物（网页图标 favicon.ico 与 `/brand/*.svg`、桌面端 icon.png/icon.ico、托盘与启动页、安装器、Android 各密度启动图标与启动屏、`docs/brand` 预览图），全库不再手写内联 SVG，也就不会再出现「同一个标各写一份、互不一致」；补齐**亮色 / 暗色两套**，并定下规则——**桌面图标（exe、快捷方式、开始菜单、托盘、安装包、Android 启动图标）始终用亮色**，软件内（浏览器标签页图标、桌面端标题栏、侧栏 logo、登录页、欢迎页）**跟随主题自动切换**（例外两处固定底色：桌面端启动页固定深色用暗色版、安装器固定亮色）；顺带修掉旧图里「电子浮在轨道环外」与横排 logo 变换不一致两处几何缺陷
- **Windows 11 Fluent 设计语言**：覆盖登录、侧栏、编辑器、设置页四大类与沉浸阅读视图，选中态左侧 3px 强调色指示条
- **浅色 / 深色双主题**：阅读视图工具栏可一键切换，主题选择持久记忆
- **自建悬停提示（不再用系统原生 title）**：全站提示统一走自建气泡，支持「标题 + 说明 + 快捷键」三段式；气泡**先测量真实尺寸再择向**，四边留 8px 安全区、箭头钉住被说明对象，任何情况下不越界、不压住被说明对象。**避让只在被说明对象的四周**：贴着对象逐边比较，对附近真实内容块做碰撞检测，按「压住对象 > 该边放不下 > 遮挡面积 > 距离」选边，必要时换边或收窄气泡——**不做外移、不画虚线引导线**；显式指定的方向（如左侧图标栏的提示固定贴按钮右侧）只要放得下就优先。设置 → 账户与外观 可在「严格避让」（宁可换边也不压内容）与「就近优先」（首选方向优先、允许轻微遮挡）之间切换。编辑器工具栏（双链 / 全屏编辑等 Vditor 自带按钮）也换成了同一套气泡，不再用 Vditor 自带的固定贴边提示。图谱节点用**光标跟随**式提示（贴着鼠标走、不遮节点），密钥字段、图谱工具条、返回轨迹等原本走系统 `title` 的地方已全部换成自建提示
- **侧栏批量折叠 / 拖拽导入**：知识库侧栏一键全部收起/展开（含实体子类与原始资料的三个二级分组，状态记忆）；原始资料分区支持从资源管理器直接拖入文件导入，落「文档」二级目录（桌面端与 Docker 网页版一致）
- **AI 工作区默认隐藏**：侧栏底部的「AI 工作区」（服务端自动生成的 `AIWorks/log` 操作日志、`AIWorks/index` 全库索引、`AIWorks/scheme` 关系库）日常不需要看，**默认不显示**；需要时在 设置 → 账户与外观 → 外观 打开「显示 AI 工作区」（开关存服务端设置 `show_ai_workspace`，多端一致，随时可关）。Agent 侧读写不受影响——这些内容仍照常生成、仍可经 MCP/CLI 读取。
- **Windows 桌面端导入只收 Markdown**：桌面端（Windows）原始资料导入仅接受 `.md` / `.markdown`，文件选择器默认只列 Markdown；拖入其他格式会被拦下并逐个点名提示，同时告诉用户出路——「其他格式请先用外置 Agent 转成 Markdown 再导入」（ZCode / Codex / Claude Code 经 MCP 读写本库）。Docker 网页版、CLI（`engram import`）不受此限，服务端接口本身不限格式；把文件直接拷进数据目录的 `原始资料/文档` 文件夹也会被文件监听即时对账（登记索引 + 排上文本提取），不用重启

## 下载与安装

三种使用方式，按机器选择：

**Windows 桌面端 · 安装包**（给非开发机器）

1. 从 [Engram Releases](https://github.com/jadesLL/Engram/releases) 下载 `Engram Setup <版本>.exe`
2. 双击安装；数据在 `%APPDATA%\@engram\desktop`，与源码版互通
3. 更新：点标题栏 `Engram` 右侧「更新」小按钮展开面板，再点「立即下载并安装」一键完成（下载 → 静默安装 → 自动重启）；也可以走 设置 → 连接与同步 → 桌面端更新 → 「检查更新」作为兜底

**Windows 桌面端 · 源码版**（自用开发机，推荐）

1. 从 **[源码版安装器（固定链接，永远最新）](https://github.com/jadesLL/Engram/releases/download/installer-latest/Engram-source-setup.exe)** 下载 `Engram-source-setup.exe`（或取仓库内 `main/scripts/install-engram.ps1` 用 PowerShell 运行），双击后全自动：下载便携 Git/Node/pnpm（免管理员，装在 `%LOCALAPPDATA%\engram`）→ 克隆源码（公开仓库无需凭据）→ 构建桌面端 → 生成桌面快捷方式并启动。该链接固定指向最新引导器、**不绑版本号**——装机逻辑（clone 地址、pnpm 版本等）更新后单独重发布即可，不必等发版（见 [GITEA-CI.md](main/docs/GITEA-CI.md)）
   **裸机可用（全新系统没装 Git / Node 也能一次装完）**：便携 Git（MinGit 2.55.0.5）与便携 Node（v22.20.0）的下载地址与版本号都内置在 `install-engram.ps1` 里，按 `npmmirror → 华为云 → 官方源` 逐个回退；下载产物按 ZIP 头与体积校验，镜像返回的错误页、被掐断的半截文件不会被当成成功。pnpm 装不上时依次退到「安装目录内私有前缀 `%LOCALAPPDATA%\engram\pnpm-home`」与华为云 registry；依赖与构建阶段的 Electron 运行时、better-sqlite3 原生模块同样默认走国内镜像（环境变量显式导出，不依赖仓库 `.npmrc`）。内网代理/自建离线镜像场景可用 `ENGRAM_MINGIT_URL` / `ENGRAM_NODE_URL` 覆盖为单一下载地址
2. 日常双击桌面「Engram」直接启动（Engram 品牌图标，无更新窗口）；更新走应用内 设置 → 连接与同步 → 桌面端更新 → 「检查更新」（增量拉源码重建重启），或手动运行 `main/scripts/update-from-source.ps1`——合 main 即更新，无需等发版。源码模式的启动程序是 Electron 官方 `electron.exe`（资源管理器/任务栏里显示的是 Electron 原子图标），构建与更新时会在同目录生成一份带 Engram 图标的 `Engram.exe`，桌面（及已存在的开始菜单）快捷方式指向它；**桌面图标丢了或显示不对**时，用 设置 → 连接与同步 → 桌面端应用 → 「重建桌面快捷方式」一键重建
3. 更新是全自动的：**依赖清单真变化时应用内会自己装依赖**（`pnpm install`，含 desktop/server 运行时依赖与 better-sqlite3 的 Electron 原生模块），不需要再去终端跑脚本；判断依据是「依赖指纹」（lockfile/workspace 配置 + 各 package.json 的依赖字段），`appId` 之类的元信息改动不会误报。源码模式默认**自动检查更新**（启动 4 秒首查，之后按自适应退避复查：连续没发现更新时 2→5→10→30→60 分钟逐级拉长（有更新待处理时每分钟复查），发现新提交或切回窗口/解除休眠立刻回到最短间隔；检查是两级探测——先用一次 `git ls-remote` 问远端提交号，只有它变了才真的 `git fetch`，日常几乎不占带宽），发现新提交时标题栏 `Engram` 右侧出现只写「更新」的小按钮（鼠标扫到即展开面板，点一下也能开合）并弹一次系统通知；点面板主按钮「立即更新并重启」即增量拉取、重建并自动重启（源码模式不自动重启，时机由你决定）。**例外**：本次更新要换 Electron 运行时时（如 Electron 35 → 36），应用内会交接给 `update-from-source.ps1` 并在退出后打开一个更新控制台窗口完成（同步依赖 → 构建 → 自动启动）——因为运行时不能在应用自己还在跑的时候替换（Windows 删不掉正在使用的 `electron.exe`，pnpm 剪枝只会删一半，留下解析不到应用的残骸）
4. 源码模式怎么确认「更新到了哪一版」：设置 → 账户与外观 → 连接与版本（高级，默认收起）→「应用版本」显示 `版本号 · 提交号 · 提交日期`（工作区有未提交改动时提交号带 `-dirty`），服务器更新/桌面端更新卡片右上角同样带提交号，「检查更新」显示本地 → 远端提交号对比。**版本号只在正式发版时变，提交号随每次更新变**，以提交号判断是否已更新到最新代码（与 Docker 镜像、安装包的版本号语义一致）
5. 卸载：应用内 设置 → 数据与存储 → 危险操作 → 「卸载 Engram」（与清库 / 清日志同组，排在设置最后一个分类），自动停止应用、删除桌面快捷方式与整个安装目录；知识库数据 `%APPDATA%\@engram\desktop` 默认保留，确认不要可勾选一并删除。也可手动运行 `main/scripts/uninstall-engram.ps1`。全局 pnpm 不受影响（若安装时全局装不上、退到了 `%LOCALAPPDATA%\engram\pnpm-home`，它随安装目录一起删除）
6. **开机自启（可选）**：设置 → 连接与同步 → 桌面端应用 → 「开机自启」打开后，登录 Windows 会**静默启动到系统托盘**——只拉起内嵌后端与托盘图标、不弹主窗口，随后双击托盘图标（或桌面快捷方式）即打开主界面；托盘右键菜单里也有同一个开关。启动项写在注册表 `HKCU\...\CurrentVersion\Run` 的 `Engram` 值（任务管理器「启动」页可禁用），应用每次启动会核对命令行、安装目录变了会自动改写；便携版不提供该项

**Docker（服务器 / NAS）**：见下方「快速开始（Docker 部署）」；更新可在 设置 → 连接与同步 → 服务器更新 一键拉镜像重建（需挂载 docker.sock）。镜像构建期会把提交号烤入 `/app/GIT_SHA`（release.yml 自动传，本地构建可 `ENGRAM_GIT_SHA=$(git rev-parse HEAD) docker compose up -d --build`），因此设置 → 账户与外观 → 连接与版本（高级，默认收起）→「应用版本」显示 `版本号 · 提交号`，一眼能看出当前跑的是哪次构建。

Docker 版同样支持**跟主分支不发版更新**：设置 → 连接与同步 → 服务器更新 → 「更新通道」选 `main`，之后每次 main 推送 CI 都会重推 `:main` 镜像，点「立即更新」即拿到主分支最新代码（版本号不变，看提交号判断）；发版前的测试就靠这条通道，验证完切回 `latest`。默认通道 `latest` 只跟正式发版。网页打开约 3 秒后自动检查更新，之后按同样的自适应退避复查（连续没发现更新时 2→5→10→30→60 分钟封顶，有更新待处理时每分钟复查；切回前台/网络恢复立刻补查）；发现新版本时右上角弹出更新提示条，展开可看 Release 更新说明，主按钮「立即更新」直接拉镜像重建并在新容器起来后自动刷新页面。

**Android 本地优先版**：APK 内置同一套 Vue 界面、仅监听 `127.0.0.1` 的本地服务、Markdown 文件库和 SQLite 索引。首次启动可直接建立空库离线使用，之后在 设置 → 连接与同步 → 多端同步 填中枢地址和成员令牌；冷启动、回到前台、本机修改或手动对账时执行一轮同步，进入后台即取消网络请求并保留待推队列。Android 只作为成员端，不运行本机 dsh/MCP、ONLYOFFICE、中枢管理、DDNS/TLS 或服务器更新；绑定后可在原生聊天抽屉使用 Docker 中枢的 Agent，会话与长任务留在 24 小时在线的服务器上。系统「分享」里的文字和文件可直接收进 `原始资料/文档`，文件选择器继续支持手机相册与系统文档源。APK 为按需发版产物，详见 [Android 文档](main/docs/ANDROID.md)。

## 快速开始（Docker 部署）

```bash
git clone https://github.com/jadesLL/Engram.git
cd Engram/main
docker compose up -d --build
```

访问 `http://<主机IP>:18080`。初始密码：`DEFAULT_PASSWORD` 环境变量非空时用其值；留空时首次登录页面直接设置。

公开仓库未发布 Docker 镜像；自建部署请从源码构建：

```bash
# Docker 镜像未公开发布（原私有 Registry 不对外）
# 需要镜像请自行构建：docker compose -f main/docker-compose.yml up -d --build
docker compose -f docker-compose.pull.yml up -d
```

**NAS 部署**（极空间 / 群晖 / 威联通等）用 `main/docker-compose.nas.yml`：宿主端口可调（默认 18080）、JWT 密钥走同目录 `.env`、卷名固定，只需 compose + `.env` 两个文件，无需克隆仓库。完整步骤与坑位见 [`main/docs/BUILDING.md`](./main/docs/BUILDING.md) 的「方式四：NAS 部署」。

Windows 桌面端安装包从 [Engram Releases](https://github.com/jadesLL/Engram/releases) 下载（`Engram Setup <版本>.exe`），更多安装方式见上文「下载与安装」。

> 想从源码自行构建，见 [`main/docs/BUILDING.md`](./main/docs/BUILDING.md)。

## 数据目录

数据都在 `./data`（映射到容器 `/data`）；DB 丢失后可从 brain/ 重建。

**备份/恢复**：设置 → 数据与存储 → 备份与恢复 → 「导出备份」一键打包整库（`wiki.db` + `brain/`）为 zip 下载；「从备份恢复」上传备份 zip 并输密码后暂存，重启服务生效（桌面端自动重启，Docker 版重启容器）。旧数据会保留一代（`wiki.db.pre-restore` / `brain.pre-restore`）便于手动回退。桌面端还可在设置里自定义数据保存位置（类 Obsidian 仓库位置，更换时旧数据自动迁移），Docker 版数据位置由 compose 卷挂载决定。

Android 数据位于应用私有目录（`brain/` 为真源，`wiki.db` 为可重建索引）；卸载 APK 会删除未同步、未导出的本机数据。Android 导出可移植 v2 备份（`manifest.json + brain/ + portable-metadata.json`，不含登录会话和同步令牌），也可导入旧版 `wiki.db + brain/` 备份并从文件重建索引。

**本地服务端口**（桌面端）：设置 → 数据与存储 → 存储位置 → 「本地服务端口」可修改内嵌后端监听端口，默认 18180（与 Docker 版 18080 互不冲突）；改动前自动预检端口占用，应用后内嵌服务以新端口重启，配置存桌面端 `config.json`。Docker 版端口仍由 compose 映射控制。

```
data/
├── brain/              # 权威源：Markdown 页面 + 原始文件
│   ├── 原始资料/        # 一级目录：上传的资料与对话沉积；文本层提取后可被检索（图片不能单独上传）
│   │   ├── 文档/        # 二级：成文的完整文件（会议纪要、调研报告、复盘、年报、教程、攻略…）；上传/新建/拖入/收集箱入库都落这里
│   │   ├── 对话/        # 二级：与 Agent 的对话沉积（save_chat 写入，可按项目再分子目录）
│   │   └── 灵感碎片/    # 二级：随手记（零散条目、想法、待办）
│   ├── 收集箱/          # 拖入的任意格式暂存区（待整理/待转换）+ 转换结果/（语义转换产物）；入库前不属于知识库
│   ├── Wiki/           # 知识库页面（概念/实体五子类/查询/归档/关系，目录固定）
│   ├── AIWorks/        # 系统区（log/log.md 操作日志、index/index.md 分类索引、scheme/relationships.md 关系库：词表关系+双链关联+待建页面；服务端自动生成，不参与检索）
│   ├── assets/         # 图片资产：assets/<页面id>/<sha1-8>-<原名>.<ext>（每个 md 内容一个目录；_unassigned/ 是历史散图收容所）
│   └── .trash/         # 回收站（软删除）
├── logs/
│   └── sync.jsonl      # 多端同步事件日志（每次推送/拉取/对账/冲突一行；保留最近 2000 条 / 7 天）
└── wiki.db             # SQLite（FTS 索引/图谱/配置/证据账本）
```

> 目录结构固定：页面只能建在 Wiki 树内；**原始资料固定三个二级目录——文档 / 对话 / 灵感碎片**，上传、拖入、新建文件与收集箱入库都落「文档」，对话留给 `save_chat`，随手记进「灵感碎片」（二级分类上线前留在 `原始资料/` 根目录的历史文件按「文档」展示，磁盘不动）；图片只能插进某个内容里，成为它的资产。
> 图片的归属由 md 正文里的 `/media/<父项id>/<文件名>` 引用决定，磁盘位置与 URL 一一对应；`wiki.db` 丢了也能从正文重建归属。历史正文里的 `/api/files/raw?path=assets%2F…` 写法与散落在 `原始资料/`、`assets/` 根目录的图片会在首次启动扫描时一次性归位（只移动、不删除）。

## 让 Agent 开始干活

1. 设置 → Agent 接入 →「接入目标」选「其他 Agent（MCP 接入）」→ 生成 Token；复制对应 Agent 的接入片段（或 CLI `login` 保存连接）
2. Agent 侧获取作业方法论：MCP `kb_guide` / `engram guide`
3. 按指南作业即可——收到提炼指令先自动索引待提炼清单（`files list --pending`），逐份提炼、写页带 `evidence` 引文，门禁与服务端日志自动兜底；提炼过的源文件在侧栏自动标「已提炼」

> 接入方纪律详见 [`main/docs/AI-CONTENT-OPERATIONS.md`](./main/docs/AI-CONTENT-OPERATIONS.md)；MCP `instructions` 会下发浓缩版。

## 本地开发

```bash
cd main
pnpm install
pnpm dev          # server :8080 + web :5173（代理到 8080）
pnpm build        # 构建全部
pnpm start        # 生产模式运行（server 托管 web/dist）
```

> 上述命令用于人工维护单一检出目录。Agent 的并行 worktree 不执行 `pnpm install`，统一按 [`main/WORKTREES.md`](./main/WORKTREES.md) 在 Docker 中验证和预览。

## 快捷键

`Ctrl+K` 搜索 · `Ctrl+N` 新建页面 · `Ctrl+S` 保存 · `[[` 插入双链 · `Ctrl+Enter` 打开光标处双链

## 技术栈

Fastify + better-sqlite3（FTS5）· Vue 3 + Vditor + vis-network · Electron（桌面端）· Docker 单容器（+ ONLYOFFICE）

## 版本与发布

- **更新日志**：[`CHANGELOG.md`](./CHANGELOG.md)——每个版本的全部新功能与变更；发版时由 CI 自动发布到 GitHub Release 正文
- **GitHub Release**：`v*` 标签自动构建，附 Windows 安装包（exe）、Android 安装包（apk）、Docker 镜像包（tar.gz）与 sha256 校验；Release 挂在本仓库 [Releases](https://github.com/jadesLL/Engram/releases)，发版是显式动作，仅按需执行
- **源码模式通道**：自用机器不依赖发版——合 main 后即可通过桌面快捷方式或应用内「检查更新」增量拉源码更新（见「下载与安装」）；Docker 部署可切「更新通道 → main」用镜像形式达到同样效果（合 main 即更新）
- **镜像**：`gitea.example.com/example/engram/engram:<版本>`（未公开发布；需要请自行构建）
- **发版流程**：详见 [`main/docs/BUILDING.md`](./main/docs/BUILDING.md)；CI/CD 维护见 [`main/docs/GITEA-CI.md`](./main/docs/GITEA-CI.md)

## 文档索引

| 文档 | 内容 |
|---|---|
| [`main/AGENTS.md`](./main/AGENTS.md) | 开发 Agent 约定（worktree、验证、合并） |
| [`main/WORKTREES.md`](./main/WORKTREES.md) | Worktree 工作流与资源隔离规则 |
| [`main/docs/BUILDING.md`](./main/docs/BUILDING.md) | 构建与部署指南（安装包生成、发版、部署） |
| [`main/docs/GITEA-CI.md`](./main/docs/GITEA-CI.md) | Gitea CI/CD、发版流程、镜像分发 |
| [`main/docs/IPV6.md`](./main/docs/IPV6.md) | IPv6 直连优先 + 隧道兜底（部署、DDNS、路由器放行） |
| [`main/docs/AI-CONTENT-OPERATIONS.md`](./main/docs/AI-CONTENT-OPERATIONS.md) | 外部 Agent 操作知识内容的纪律总纲 |
| [`main/desktop/README.md`](./main/desktop/README.md) | Windows 桌面端（本地内嵌、打包） |
