# AI 协作约定（LLM Wiki 知识库）

本文件区分两类 AI，规则不能混用：

## 软件开发 Agent（修改源码 / 配置 / 测试 / 构建部署）

- 开始开发前必须先完整读取根目录 `WORKTREES.md`；**软件开发流程以该文件为唯一来源**，本文件不重复维护其具体步骤。
- 功能开发、缺陷修复和应用部署必须严格执行 `WORKTREES.md` 的 worktree 隔离、串行合并、验证与清理规则。
- 仅维护 `AGENTS.md`、`WORKTREES.md` 等协作流程文档时，使用独立文档 worktree，但不需要创建应用容器、数据卷或重新部署应用；若同时修改应用代码，则仍执行完整开发流程。
- 本节不适用于应用运行期间的 Dream Cycle、入库管线或经 MCP 读写知识内容的 Agent。

## 知识库内容操作 AI

应用内 Dream Cycle / 入库管线，以及外部 Agent（Claude Code、Cursor、ZCode 等经 MCP 接入）对知识内容进行读写时，都遵守同一条纪律：**先读操作日志，动手后写操作日志**。保证 AI 对知识库内容的操作全部可知。

## 操作日志 = 知识内容操作记录 + 索引

- **位置**：`data/brain/Wiki/log.md`（frontmatter 标题「操作日志」）。
- **形态**：时间**倒序**（越新越靠上）。每条一行：
  ```
  - YYYY-MM-DD HH:MM:SS 动作：细节
  ```
  正文顶部是 `# 操作日志` 标题，新条目插在标题正下方、旧条目之上。
- **它是知识内容操作 AI 的唯一流水，也是这类 AI 读写知识内容前必须先读的索引。**
- **原始不提炼**：条目保持原始一行式，不蒸馏、不汇总成状态看板。原始资料（`原始资料/` 下，含对话/纪要）走正常入库管线，不做绕过提炼的笔记类型。
- **不再有独立文档**：Dream Cycle 运行摘要、实体升级、合并、删除、批量处理失败等 AI 副产物**一律记进操作日志**，不再在 `AIWorks/log/` 下另建独立文件（历史存量已由启动迁移自动并入）。

## 动手前 / 动手后

1. **先读**：做任何知识内容写操作前，先读 `Wiki/log.md`，了解最近 AI 都干了什么、当前状态。
2. **后写**：知识内容操作完成后，追加一条到 `Wiki/log.md`（倒序，新的在上）。
   - 应用内 AI：由 `appendWikiLog(action, detail)`（`server/src/pipeline/indexFile.ts`）自动完成。
   - 外部 Agent（MCP）：用 `write_page` 写 `Wiki/log.md`，把新条目插在 `# 操作日志` 标题正下方。

## 应用内自动写入点（appendWikiLog）

| 触发 | 动作 |
|---|---|
| 新建页面 | `新建页面` |
| 归档 / 取消归档 | `归档` / `取消归档` |
| 删除页面 | `删除` |
| 合并页面 | `合并`（原页已归档） |
| 死链批量建页 | `新建页面` |
| Dream Cycle 运行 | `Dream Cycle`（死链/疑似重复/矛盾/待丰富/过期/来源单一/待补章节/实体升级 计数全保留） |
| 实体升级批次 | `实体升级`（所有实体全列） |
| 批量处理失败 | `批量处理失败`（所有错误行全保留） |
| 一键清除 | `清除`（重置操作日志） |

> Dream Cycle 定时由 `server/src/dream/scheduler.ts` 触发（默认每日 03:00，`dream_cron` 可配），也可经 `POST /api/dream/run` 手动触发。运行结果同时落库为「整理报告」（`reports` 表，供整理报告页处理），操作日志只记运行摘要。

## 清空

`POST /api/settings/wipe-ai-logs`（需密码）会清空 `AIWorks/log/` 残留文件并重置 `Wiki/log.md` 为初始状态（仅保留本次清空记录）。正常使用下 `AIWorks/log/` 已永久为空。
